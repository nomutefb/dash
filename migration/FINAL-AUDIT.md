# 최종 감사 — 2026-09-11

## 완료

- 이전 오치환 복구 11개 파일의 변경 사유와 이전 해시를 확인한 뒤 해당 파일만 manifest 갱신.
- 앱 이름, 이미지/유보석/대학로 등의 일반 문구와 공개 공연명 복구를 확인. HTML 인라인 CSS는 이전본과 동일했고, HTML 골격 차이는 apple-mobile-web-app-title 복구뿐이었다.
- 직원 37명의 기존 가명 관계를 과거 로컬 원본의 동일한 담당자 행과 메모리에서 대조했다. 다른 행 필드가 모두 일치함을 확인했고 대응표는 저장하지 않았다.
- 일정·로그·설명문 등 23개 파일의 잔존 이름을 기존 가명으로 치환. 이름이 겹치는 공개 공연명 4개는 보존. 결과와 해시는 audit-privacy-repairs.json에 기록.
- 최종 잔존 검사에서 위 직원명 기준 추가 수정 대상 0개. 공개 공연명 및 테스트용 일반 표현은 제외한다. 전체 회원/자유서술 개인정보에 대한 완전한 익명성 증명은 아니다.
- 변경 구조화 자료 12개(HTML 내장 DB 포함)의 행 구조·키·값 타입과 수치 401,356개를 비교. 차이는 승인된 가명 치환·미지정 표시 복구 1,258개 문자열뿐이다. 금액·연결키·행 구조의 예상 밖 변경 0개.
- src/styles/yeulmaru.css의 상대 이미지 경로를 /image/pets/pet_crab.png로 수정. 실제 자산 존재 확인.
- 참조 검사기의 /src/main.tsx 해석 경로를 root/src로 수정. 193개 참조 중 184개 해결, 플랫폼/인증/main.tsx 관련 9개는 미해결로 유지.
- 원본 MISO 화면과 로컬 로그인 화면의 기존 증빙 이미지를 다시 확인했다. 이번 세션에서 새 로그인 이후 브라우저 검증을 수행한 것은 아니다.

## 검증 근거

- local-verification.json: 전체 해시·행 수·회원/예매 연결·샤드·JS/HTML 구문.
- audit-data-integrity.json: b26d227 대비 최종 구조화 데이터의 정확한 변경 제한 검사.
- audit-final-delta.json: 개인정보 추가 보정 전 오치환 복구의 코드/HTML 구조 검사. 시점이 다르므로 최종 개인정보 보정 결과와 혼동하지 않는다.
- audit-code-structure.json: 이전 MISO 캡처 대비 구문 트리 검사 기록. finalLocalSha256는 현재 파일 해시이고, 기존 비교 내용 자체는 역사적 기록이다.
- audit-live-files.json: sourceSha256는 이전 MISO 캡처이며 localSha256만 최종 파일로 갱신했다. 새 원격 조회를 뜻하지 않는다.
- audit-repairs.json: 중간 복구 해시는 이력으로 보존하고 finalFiles에 마지막 해시 기록.

## 업로드와 실행 제한

GitHub 플러그인은 nomutefb로 연결되어 있으나 PC의 Git 인증은 muteno다. push --dry-run이 403으로 실패했고, 비대화형 nomutefb 인증 확인에서도 저장된 인증을 찾지 못했다. 원격 main은 5d231f4fa79744c41b5f3653e583d652266a454c의 초기 README다.

원본 이름이 포함된 이전 로컬 커밋을 공개 원격에 밀어 올리지 않는다. 업로드용 브랜치는 정리된 최종 트리를 원격 초기 커밋의 자식으로 만들어 일반 push가 가능하도록 구성한다. 이전 로컬 main과 백업 브랜치는 보존한다. 강제 push나 원본 MISO 변경은 하지 않는다.

별도 런타임/인증 구성과 src/main.tsx 확보가 필요하다. 로그인 이후 전 화면 동등성, 실제 CRUD/DB 재가져오기, 수집기, Microsoft 로그인, PWA 설치는 미검증이다. 현재 원본과의 데이터 최신성도 이번 감사 범위가 아니다.
