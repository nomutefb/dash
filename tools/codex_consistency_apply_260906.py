from pathlib import Path
import json,base64,zlib,re,subprocess,hashlib
sale=Path('api/ym-sales-lib.js'); sales=sale.read_text(encoding='utf-8')
assert hashlib.sha256(sales.encode()).hexdigest()=="963c953ebc19f2bf53c4a82b406dce2c2cce545efff701c2c4e51d03dee99b3a"
updatedSales=sales
for old,new in json.loads("[[\"function toView(d) {\",\"function toView(d, canonical) {\"],[\"if (MAP[i][0] === \\\"명칭\\\" && nz(d[\\\"판매명칭\\\"]))\",\"if (MAP[i][0] === \\\"명칭\\\" && nz(d[\\\"판매명칭\\\"]) && (!canonical || !nz(d[\\\"정본명\\\"])))\"],[\"  return o;\\n}\\nfunction allMaster\",\"  if (canonical && nz(d[\\\"회차수\\\"]) !== \\\"\\\") o[\\\"총회차\\\"] = String(d[\\\"회차수\\\"]);\\n  return o;\\n}\\nfunction allMaster\"],[\"var recs = allMaster(app, col), out = [];\",\"var recs = allMaster(app, col), out = [], dc = col(\\\"ymdata\\\"), canonical = String(dc.name || dc) === \\\"ymdata_dev\\\";\"],[\"out.push(toView(d));\",\"out.push(toView(d, canonical));\"]]"):
 assert updatedSales.count(old)==1,old
 updatedSales=updatedSales.replace(old,new)
p=Path('public/standalone.html'); original=p.read_text(encoding='utf-8')
ref=Path('tools/plan_ui5_260906.py'); refSource=ref.read_text(encoding='utf-8')
encoded=re.search(r'b64decode\("([A-Za-z0-9+/=]+)"\)',refSource).group(1)
module=zlib.decompress(base64.b64decode(encoded)).decode('utf-8')
assert original.count(module.strip())==1,'live module changed'
newModule=module.replace("+_ypYearSel('_ypBizSetYear(this.value)')+","+_ypYearSel('_ypBizSetYear(this.value)')+(_yp.env==='dev'?'<button class=\"adm-btn\" onclick=\"_ymCheckDB()\">DB 일치 검사</button>':'')+")+"\n/* [codex-consistency-260906] Current DB is the common display source. */\nvar _ymViewEpoch=0, _ymViewTimer=null;\nfunction _ymDevConsistency(){ return (typeof _yp!=='undefined'&&_yp.env==='dev') || (typeof location!=='undefined'&&location.pathname.indexOf('/service/coder/preview/')===0); }\nfunction _ymInvalidateViews(path,body){\n if(!_ymDevConsistency())return;\n var relevant=/^\\/api\\/ym\\/plan\\//.test(path)||/^\\/api\\/programs(?:\\/|$)/.test(path)||/^\\/api\\/sheet\\/program/.test(path)||(/^\\/api\\/ops(?:\\/|$)/.test(path)&&/프로그램마스터|판매설정|일일실적|사업비|공연마스터|전시마스터|일일입력|전시일일|세부운영관리대장/.test(String(body&&body.sheet||'')));\n if(!relevant)return;\n _ymViewEpoch++; if(typeof _apiWriteSeq!=='undefined')_apiWriteSeq++;\n if(typeof _plJobs!=='undefined')Object.keys(_plJobs).forEach(function(k){if(/^\\/api\\/(ops|programs|records)/.test(k))delete _plJobs[k];});\n if(typeof _pmSheetP!=='undefined')_pmSheetP=null;\n if(typeof _pmLoading!=='undefined')_pmLoading=null;\n if(typeof _PM!=='undefined')_PM=null;\n if(typeof _BPL!=='undefined')_BPL=null;\n if(typeof _PM_UN!=='undefined')_PM_UN=null;\n if(typeof _PM_BY_ID!=='undefined')_PM_BY_ID={};\n if(typeof _BPL_BY_KEY!=='undefined')_BPL_BY_KEY={};\n if(typeof _salesBuildMemo!=='undefined')_salesBuildMemo=null;\n if(typeof _salesState!=='undefined'&&_salesState)['master','daily','rounds','ops','group','_opsIdx'].forEach(function(k){_salesState[k]=null;});\n if(typeof _bizState!=='undefined'&&_bizState)_bizState.raw=null;\n if(typeof _anaState!=='undefined'&&_anaState){_anaState._exDaily=null;_anaState._exMaster=null;}\n if(typeof _dailyState!=='undefined'&&_dailyState)['master','daily','rounds','exMaster','exDaily','programs'].forEach(function(k){if(Object.prototype.hasOwnProperty.call(_dailyState,k))_dailyState[k]=null;});\n if(typeof _srailGen!=='undefined')_srailGen++;\n if(typeof _srailBusy!=='undefined')_srailBusy=false;\n if(_ymViewTimer)clearTimeout(_ymViewTimer);\n _ymViewTimer=setTimeout(function(){\n  _ymViewTimer=null;\n  var work=[];\n  if(typeof _pmLoad==='function')work.push(_pmLoad());\n  if(typeof loadData==='function')work.push(loadData());\n  Promise.all(work).then(function(){if(typeof _bizInlineRender==='function'&&document.getElementById('biz-main'))_bizInlineRender();}).catch(function(e){console.warn('[DB display refresh]',e);});\n  if(typeof _srailInit==='function')_srailInit(true,null,true);\n },300);\n}\nfunction _ymCompareDB(data){\n var issues=[],checks=0, master={},biz={};\n function text(v){return String(v==null?'':v).trim();}\n function add(id,field,base,view){issues.push({id:id,field:field,base:text(base),view:text(view)});}\n function compare(id,field,base,view,numeric){checks++;if(numeric&&text(base)!==''&&text(view)!==''){if(Number(base)===Number(view))return;}if(text(base)!==text(view))add(id,field,base,view);}\n function index(rows,key,target,label){(rows||[]).forEach(function(r){var id=text(r[key]);if(!id){add('',label+' ID 없음','','');return;}if(target[id])add(id,label+' ID 중복','1','2+');else target[id]=r;});}\n index(data.master,'프로그램ID',master,'마스터'); index(data.biz,'사업NO',biz,'사업');\n (data.sales||[]).forEach(function(r){var id=text(r.프로그램ID),m=master[id];if(!m){add(id,'판매 → 마스터 참조','마스터 행','없음');return;}compare(id,'판매 명칭',m.정본명||m.판매명칭,r.명칭);compare(id,'판매 사업코드',m.사업코드,r.사업코드);if(text(m.회차수)!=='')compare(id,'판매 회차',m.회차수,r.총회차,true);});\n (data.programs||[]).forEach(function(r){var id=text(r.프로그램ID),m=master[id];if(!m){add(id,'프로그램 → 마스터 참조','마스터 행','없음');return;}compare(id,'프로그램 명칭',m.정본명,r.정본명);compare(id,'프로그램 사업코드',m.사업코드,r.사업코드);compare(id,'프로그램 수입',m.표시_수입,r.수입,true);});\n var bm=[['사업명','사업명'],['실지출','전표실적'],['수입','정산서매출'],['판매수수료','판매수수료'],['유료관객','유료인원'],['무료관객','초대인원']];\n (data.planBiz||[]).forEach(function(r){var id=text(r.사업코드),m=biz[id];if(!m){add(id,'사업 → 원장 참조','사업 행','없음');return;}bm.forEach(function(pair){compare(id,'사업 '+pair[0],m[pair[1]],r[pair[0]],pair[0]!=='사업명');});});\n (data.daily||[]).forEach(function(r){var id=text(r.프로그램ID),m=master[id];if(!m){add(id,'일일실적 → 마스터 참조','마스터 행','없음');return;}compare(id,'일일실적 명칭',m.정본명,r.명칭);compare(id,'일일실적 사업코드',m.사업코드,r.사업코드);});\n return {checks:checks,issues:issues};\n}\nasync function _ymCheckDB(){\n if(!_ymDevConsistency())return;\n _ypModal('yp-db-check','<div class=\"modal-bg show\" style=\"z-index:9800\"><div class=\"modal\" style=\"max-width:1000px;width:96vw;max-height:85vh;overflow:auto;padding:24px\"><button class=\"modal-x\" aria-label=\"일치 검사 닫기\" onclick=\"_ypClose(&quot;yp-db-check&quot;)\">✕</button><h3>DB 일치 검사</h3><div id=\"yp-db-check-result\">현재 DB 조회 중</div></div></div>');\n try{\n  var paths=['/api/ops?sheet='+encodeURIComponent('프로그램마스터')+'&fresh=1&nocache=1','/api/ops?sheet='+encodeURIComponent('사업비')+'&fresh=1&nocache=1','/api/ops?sheet='+encodeURIComponent('판매설정')+'&fresh=1&nocache=1','/api/ym/plan/programs','/api/ym/plan/biz','/api/ops?sheet='+encodeURIComponent('일일실적')+'&fresh=1&nocache=1'];\n  var results=await Promise.all(paths.map(function(path){return api('GET',path);}));\n  results.forEach(function(r){if(!r||!Array.isArray(r.rows))throw new Error('조회 응답 누락');});\n  var result=_ymCompareDB({master:results[0].rows,biz:results[1].rows,sales:results[2].rows,programs:results[3].rows,planBiz:results[4].rows,daily:results[5].rows});\n  var h='<p>현재 DB 기준 '+result.checks+'개 항목 검사 · 불일치 '+result.issues.length+'건</p><p>이름·사업코드·회차·동일 의미의 사업 금액과 관객·프로그램 수입·일일실적 참조를 비교합니다. 사업 합계와 개별 프로그램 값은 서로 같은 값으로 간주하지 않습니다.</p>';\n  if(result.issues.length)h+='<table class=\"adm-tbl\"><thead><tr><th>ID</th><th>항목</th><th>DB 기준</th><th>조회 값</th></tr></thead><tbody>'+result.issues.slice(0,200).map(function(x){return '<tr><td>'+escapeHtml(x.id)+'</td><td>'+escapeHtml(x.field)+'</td><td>'+escapeHtml(x.base||'(빈칸)')+'</td><td>'+escapeHtml(x.view||'(빈칸)')+'</td></tr>';}).join('')+'</tbody></table>'+(result.issues.length>200?'<p>앞의 200건 표시</p>':'');\n  _ypBody('yp-db-check-result',h);\n }catch(e){_ypBody('yp-db-check-result','조회 실패: '+escapeHtml(_ypErr(e)));}\n}\n"
updated=original.replace(module.strip(),newModule.strip())
for old,new in json.loads("[[\"_inf=_apiInflight[path]||\",\"_inf=(_apiInflight[path]&&((typeof _ymDevConsistency!=='function'||!_ymDevConsistency())||_apiInflight[path].w===_apiWriteSeq)?_apiInflight[path]:null)||\"],[\"const r=await fetch(method==='GET'||localDbPath?path:`${API}${path}`,o);if(!r.ok)throw new Error(`${r.status}: ${await r.text()}`);return r.json();\",\"const r=await fetch(method==='GET'||localDbPath?path:`${API}${path}`,o);if(!r.ok)throw new Error(`${r.status}: ${await r.text()}`);const result=await r.json();if(result&&result.ok!==false&&typeof _ymInvalidateViews==='function')_ymInvalidateViews(path,body);return result;\"],[\"_pmLoading=_pmSheetRows().then(function(list){\",\"var __ymEpoch=typeof _ymViewEpoch==='number'?_ymViewEpoch:0;\\n_pmLoading=_pmSheetRows().then(function(list){\"],[\"  _PM=list.map(function(p){\",\"  if(typeof _ymDevConsistency==='function'&&_ymDevConsistency()&&__ymEpoch!==_ymViewEpoch)return _pmLoad();\\n  _PM=list.map(function(p){\"]]"):
 assert updated.count(old)==1,old
 updated=updated.replace(old,new)
for source in [updatedSales,newModule]:subprocess.run(['node','--check'],input=source,text=True,check=True)
subprocess.run(['node','-e',"const vm=require('vm'),fs=require('fs'),assert=require('assert');const c={module:{exports:{}},console};vm.runInNewContext(fs.readFileSync(0,'utf8'),c);const row={'프로그램ID':'p','판매구분':'공연','정본명':'A','판매명칭':'A-2','회차수':'0','총회차':'2'};const app={findRecordsByFilter:()=>[{publicExport:()=>({data:row})}]};assert.equal(c.list(app,()=>({name:'ymdata_dev'}))[0]['명칭'],'A');assert.equal(c.list(app,()=>({name:'ymdata_dev'}))[0]['총회차'],'0');assert.equal(c.list(app,()=>({name:'ymdata'}))[0]['명칭'],'A-2');assert.equal(row['판매명칭'],'A-2');console.log('PASS canonical name and round zero; DB source and prod unchanged');"],input=updatedSales,text=True,check=True)
subprocess.run(['node','-e',"const vm=require('vm'),fs=require('fs'),a=require('assert');const c={console,location:{pathname:'/service/coder/preview/test/'},setTimeout:()=>1,clearTimeout:()=>{},_apiWriteSeq:1,_PM:[{id:'old'}],_BPL:[],_pmSheetP:{},_pmLoading:{},_salesState:{master:{}},_plJobs:{'/api/ops?sheet=x':{}},_anaState:{}};vm.runInNewContext(fs.readFileSync(0,'utf8'),c);c._ymInvalidateViews('/api/ym/plan/program',{});a.equal(c._PM,null);a.equal(c._salesState.master,null);a.equal(c._ymViewEpoch,1);a.equal(c._apiWriteSeq,2);a.equal(Object.keys(c._plJobs).length,0);c._ymInvalidateViews('/api/presence',{});a.equal(c._ymViewEpoch,1);c.location.pathname='/published';c._ymInvalidateViews('/api/ym/plan/program',{});a.equal(c._ymViewEpoch,1);let data={master:[{'프로그램ID':'p','정본명':'A','사업코드':'b','회차수':'0','표시_수입':'0'}],sales:[{'프로그램ID':'p','명칭':'A','사업코드':'b','총회차':'0'}],programs:[{'프로그램ID':'p','정본명':'A','사업코드':'b','수입':'0'}],biz:[],planBiz:[],daily:[{'프로그램ID':'p','명칭':'A','사업코드':'b'}]};a.equal(c._ymCompareDB(data).issues.length,0);data.sales[0]['명칭']='A-2';a.equal(c._ymCompareDB(data).issues.length,1);console.log('PASS invalidation scope and ID-based consistency checks');"],input=newModule,text=True,check=True)
subprocess.run(['node','-e',"const vm=require('vm'),fs=require('fs'),a=require('assert');let fetches=0;const c={console,window:{},_plJobs:{},_apiInflight:{'/api/ops?sheet=x':{p:Promise.resolve({old:true}),w:1}},_apiWriteSeq:2,_apiWrites:0,_apiClone:x=>x,_ymDevConsistency:()=>true,password:'',sessionStorage:{getItem:()=>''},fetch:async()=>{fetches++;return {ok:true,json:async()=>({fresh:true})}}};vm.runInNewContext(fs.readFileSync(0,'utf8'),c);(async()=>{let r=await c.api('GET','/api/ops?sheet=x');a.equal(r.fresh,true);a.equal(fetches,1);c._apiInflight['/api/ops?sheet=x']={p:Promise.resolve({shared:true}),w:2};r=await c.api('GET','/api/ops?sheet=x');a.equal(r.shared,true);a.equal(fetches,1);console.log('PASS pre-write GET is not reused after write; current generation shares GET');})().catch(e=>{console.error(e);process.exit(1)});"],input="async function api(method,path,body){\n  if(window._QA)return _qaApi(method,path,body);   // [QA] 실네트워크 차단 — 목데이터만(실API·실데이터 미접촉)\n  // [로그인 프리로드] 같은 경로 GET의 선행 응답을 1회 재사용(신선할 때만) — 만료·실패는 아래 정상 fetch 폴백(회귀 0)\n  if(method==='GET'&&_plJobs[path]){\n    var _pl=_plJobs[path]; delete _plJobs[path];   // 1회 소비 — 이후 같은 경로(수동 새로고침·syncReload)는 항상 네트워크\n    // [평의회1] records↔lastmod 에폭 짝 — lastmod 프리로드가 이미 죽어 있으면(실패·만료) records 워밍도 버리고 둘 다\n    //   지금 네트워크로 정렬(화면=t0·배지 기준선=t1로 갈라지는 사각 차단). 미착(pending)은 h2 같은 연결이라 비대칭 스톨이\n    //   사실상 없고(전환·슬립은 둘 다 죽어 둘 다 폴백 = 정렬), 정상 도착분은 _refreshSeenMod의 .v 경로가 t0 값을 쓴다.\n    if(path==='/api/records'){ try{ var _lm=_plJobs['/api/lastmod']; if(_lm&&(_lm.err||(Date.now()-_lm.t)>=_PL_TTL))_pl=null; }catch(_e){} }\n    if(_pl&&(Date.now()-_pl.t)<_PL_TTL){\n      // [평의회5] 스톨 방어 — 죽은-pending 소켓(잠금 방치 중 네트워크 전환·슬립 복귀)은 TTL 검사로 못 끊는다 →\n      //   8초 안에 안 오면 버리고 아래 정상 fetch(오늘의 경로)로. 최악 추가 대기 8초 상한(구: 소켓 포기까지 수 분 무표시).\n      //   타임아웃은 reject가 아니라 센티널 resolve — 패배한 타이머가 나중에 터져도 unhandledrejection(관리자 오류 토스트) 0.\n      try{ var _plto={}; var _plgot=await Promise.race([_pl.p,new Promise(function(rs){setTimeout(rs,8000,_plto);})]); if(_plgot!==_plto) return _plgot; }\n      catch(_e){}   // 프리로드 실패 = 아래 정상 fetch 폴백\n    }\n  }\n  // [260905 통합⑱] 같은 GET 이 이미 날아가는 중이면 그 응답을 복사해 쓴다(부팅 때 일일실적·판매설정 3번씩 겹치던 낭비 제거). 실패하면 아래 정상 fetch.\n  if(method==='GET'){ var _bp=path.replace(/[?&]fresh=1$/,''); var _inf=(_apiInflight[path]&&((typeof _ymDevConsistency!=='function'||!_ymDevConsistency())||_apiInflight[path].w===_apiWriteSeq)?_apiInflight[path]:null)||((_bp!==path&&_apiInflight[_bp]&&_apiInflight[_bp].w===_apiWriteSeq)?_apiInflight[_bp]:null);   // [⑱-2] fresh=1 도 그 사이 쓰기가 없었으면 합친다\n    if(_inf){ try{ var _shv=await _inf.p; return _apiClone(_shv); }catch(_e){} } }\n  const headers={'Content-Type':'application/json','X-App-Password':password};\n  // 서브 admin: 본인 PIN을 헤더에 동봉 (Worker가 검증해 admin 권한 부여)\n  const subPin=sessionStorage.getItem('subAdminPin');\n  if(subPin)headers['X-Sub-Admin-PIN']=subPin;\n  // [260812] 회계 담당자: 같은 문법으로 본인 PIN 동봉 — Worker가 담당자 시트 `회계여부`로 재검증해\n  //   **운영_사업비 쓰기만** 열어준다(다른 시트·다른 경로는 종전 admin 게이트 그대로).\n  const acctPin=sessionStorage.getItem('acctPin');\n  if(acctPin)headers['X-Acct-PIN']=acctPin; try{var _yu=sessionStorage.getItem('myApplicant')||'';if(_yu)headers['X-Ym-User']=encodeURIComponent(_yu);}catch(_eu){} /* [260906 수정이력] */\n  const o={method,headers};\n  if(body)o.body=JSON.stringify(body);\n  const _w=(method!=='GET'); if(_w){_apiWrites++; if(/^\\/api\\/(records|ops|sheet|messages|programs|ym\\/)/.test(path))_apiWriteSeq++;}   // [⑱-2] 쓰기 순번 [⑱-4] 자료 표 쓰기만(presence 같은 신호는 제외)\n  try{\n     const localLlmPath=/^\\/(?:api\\/blog\\/(?:dispatch|draft)|api\\/promo\\/(?:auto-run|auto-latest)|api\\/content\\/(?:kakao|structure|ocr|logo)|api\\/monitor\\/scan|api\\/(?:hwp|office|press|anydoc|slim)\\/dispatch)/.test(path);\n      const localDbPath=/^\\/api\\/(?:records(?:\\/|$)|records-delete$|ops(?:\\/|$)|sheet\\/|qa(?:\\/|$)|messages(?:\\/|$)|config$|memo$|presence$)/.test(path);\n     if(localLlmPath)return (await misoLlmRoute(method,path,body)).json();\n     if(method==='GET'){   // [260905 통합⑱] GET 은 in-flight 표에 올려 두고, 끝나면 내린다\n       var _pf=fetch(path,o).then(async function(r){ if(!r.ok)throw new Error(r.status+': '+(await r.text())); return r.json(); });\n       _apiInflight[path]={p:_pf,w:_apiWriteSeq};\n       try{ return await _pf; } finally{ if(_apiInflight[path]&&_apiInflight[path].p===_pf)delete _apiInflight[path]; }\n     }\n     const r=await fetch(method==='GET'||localDbPath?path:`${API}${path}`,o);if(!r.ok)throw new Error(`${r.status}: ${await r.text()}`);const result=await r.json();if(result&&result.ok!==false&&typeof _ymInvalidateViews==='function')_ymInvalidateViews(path,body);return result;\n  }finally{ if(_w)_apiWrites=Math.max(0,_apiWrites-1); }   // finally 필수 — 예외에 감산이 새면 영구 busy = 자동 탑재 전면 사망(평의회6 경고)\n}\n\n// (isSuperAdmin 제거됨 — 슈퍼admin 개념 폐기, 권한은 role==='admin'로 통일)\n\n// Excel 시리얼(1899-12-30 기준) → \"YYYY-MM-DD\" 변환. 텍스트 날짜('2026-06-30'/'2026.6.30')도 허용\n// — 관리자 CRUD/시트 수기 편집으로 시리얼 대신 문자열이 저장돼도 표시가 깨지지 않게 (260610)\n",text=True,check=True)
backup=Path('backups/codex-consistency-260906');backup.mkdir(parents=True,exist_ok=True)
for name,contents in [('standalone.html',original),('ym-sales-lib.js',sales),('plan_ui5_260906.py',refSource)]:
 with (backup/name).open('x',encoding='utf-8') as f:f.write(contents)
assert p.read_text(encoding='utf-8')==original and sale.read_text(encoding='utf-8')==sales
sale.write_text(updatedSales,encoding='utf-8')
p.write_text(updated,encoding='utf-8')
newEncoded=base64.b64encode(zlib.compress(newModule.strip().encode())).decode()
ref.write_text(refSource.replace(encoded,newEncoded),encoding='utf-8')
print('APPLIED canonical sales name/rounds, shared refresh, DB comparison UI; no DB writes; no publish')
