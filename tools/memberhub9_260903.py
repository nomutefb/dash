# -*- coding: utf-8 -*-
# [260903] v310 지표 띠 숫자 카운트업(사업현황·판매현황) — 모듈 함수 2곳(본체+참조본 동시) + 본체 훅 3곳
import io,json
RM=json.loads("[[\"function _memHubCountUp(el,dur){\\n  if(!el||typeof _rvOff!=='function'||_rvOff())return;\\n  var txt=(el.__cuText!=null)?el.__cuText:el.textContent; el.__cuText=txt;\\n  var toks=[],re=/\\\\d[\\\\d,]*(?:\\\\.\\\\d+)?/g,m; while((m=re.exec(txt))){ var raw=m[0],v=parseFloat(raw.replace(/,/g,'')),dec=(raw.split('.')[1]||'').length,pct=txt.charAt(m.index+raw.length)==='%'; if(!(v>=100||pct))continue; toks.push({s:m.index,e:m.index+raw.length,v:v,dec:dec,comma:raw.indexOf(',')>=0}); }\\n  if(!toks.length)return;\\n  dur=dur||900; var t0=performance.now(); if(el.__cuRaf)cancelAnimationFrame(el.__cuRaf);\\n  function fmt(n,t){ var x=n.toFixed(t.dec); if(t.comma||t.dec===0&&n>=1000){ var p=x.split('.'); p[0]=p[0].replace(/\\\\B(?=(\\\\d{3})+(?!\\\\d))/g,','); x=p.join('.'); } return x; }\\n  (function step(now){ var p=Math.min(1,(now-t0)/dur), e=1-Math.pow(1-p,3), out='',pos=0; toks.forEach(function(t){ out+=txt.slice(pos,t.s)+fmt(t.v*e,t); pos=t.e; }); out+=txt.slice(pos); el.textContent=out; if(p<1)el.__cuRaf=requestAnimationFrame(step); else el.__cuRaf=0; })(t0);\\n  setTimeout(function(){ if(el.__cuRaf){ cancelAnimationFrame(el.__cuRaf); el.__cuRaf=0; } el.textContent=txt; },dur+80);   /* 탭이 가려져 rAF 가 멈춰도 최종값은 반드시 쓴다 */\\n}\", \"function _memHubCountUp(el,dur,minV){\\n  if(!el||typeof _rvOff!=='function'||_rvOff())return;\\n  /* 값 칸 안에 부연 <span>(「(잠정)」「(3년 대비 90% 수준)」)이 있으면 숫자가 든 첫 글자 노드만 센다 — 마크업 보존 */\\n  var tgt=el; if(el.nodeType===1&&el.children.length){ tgt=null; for(var ci=0;ci<el.childNodes.length;ci++){ var cn=el.childNodes[ci]; if(cn.nodeType===3&&/\\\\d/.test(cn.nodeValue)){ tgt=cn; break; } } if(!tgt)return; }\\n  var txt=(el.__cuText!=null)?el.__cuText:tgt.textContent; el.__cuText=txt; if(minV==null)minV=100;\\n  var toks=[],re=/\\\\d[\\\\d,]*(?:\\\\.\\\\d+)?/g,m; while((m=re.exec(txt))){ var raw=m[0],v=parseFloat(raw.replace(/,/g,'')),dec=(raw.split('.')[1]||'').length,pct=txt.charAt(m.index+raw.length)==='%'; if(!(v>=minV||pct))continue; toks.push({s:m.index,e:m.index+raw.length,v:v,dec:dec,comma:raw.indexOf(',')>=0}); }\\n  if(!toks.length)return;\\n  dur=dur||900; var t0=performance.now(); if(el.__cuRaf)cancelAnimationFrame(el.__cuRaf);\\n  function fmt(n,t){ var x=n.toFixed(t.dec); if(t.comma||t.dec===0&&n>=1000){ var p=x.split('.'); p[0]=p[0].replace(/\\\\B(?=(\\\\d{3})+(?!\\\\d))/g,','); x=p.join('.'); } return x; }\\n  (function step(now){ var p=Math.min(1,(now-t0)/dur), e=1-Math.pow(1-p,3), out='',pos=0; toks.forEach(function(t){ out+=txt.slice(pos,t.s)+fmt(t.v*e,t); pos=t.e; }); out+=txt.slice(pos); tgt.textContent=out; if(p<1)el.__cuRaf=requestAnimationFrame(step); else el.__cuRaf=0; })(t0);\\n  setTimeout(function(){ if(el.__cuRaf){ cancelAnimationFrame(el.__cuRaf); el.__cuRaf=0; } tgt.textContent=txt; },dur+80);   /* 탭이 가려져 rAF 가 멈춰도 최종값은 반드시 쓴다 */\\n}\"], [\"function _memberHubPurge(){\", \"/* [260903 운영자 「숫자 카운트를 사업현황·판매현황 지표 띠에도」] 덱 전환·대시보드 진입 때 보이는 면의 지표 띠 값 4칸을 같은 곡선으로.\\n   호출 = _bizDeckGo(캐시 복원·새로 그림 두 경로)·_mvApply(biz 진입). 예열(_bizDeckPrewarmBusy) 중엔 안 한다. 「18.0건」「9.3억」처럼 100 미만도 센다(minV 0). */\\nfunction _bizStripCountUp(){\\n  try{\\n    if(window._bizDeckPrewarmBusy)return; if(typeof _mainView!=='undefined'&&_mainView!=='biz')return;\\n    var main=document.getElementById('biz-main'); if(!main||main.offsetParent===null)return;\\n    main.querySelectorAll('.bizm-strip .cell .val').forEach(function(v){ if(v.offsetParent===null)return; _memHubCountUp(v,900,0); });\\n  }catch(_e){}\\n}\\nfunction _memberHubPurge(){\"]]")
RB=json.loads("[[\"       try{_bizFitViewport();}catch(_e2){}\\n       if(k==='ov')setTimeout(function(){try{_srailAlignTop();}catch(_e3){}} ,0);\\n       return;\", \"       try{_bizFitViewport();}catch(_e2){}\\n       try{ if(typeof _bizStripCountUp==='function')_bizStripCountUp(); }catch(_e5){}   /* [260903 지표 띠 카운트업] 캐시 복원 면 */\\n       if(k==='ov')setTimeout(function(){try{_srailAlignTop();}catch(_e3){}} ,0);\\n       return;\"], [\"    try{_srailAlignTop();}catch(_e){} try{_bizFitViewport();}catch(_e){}   /* [260902 좌측 틀 통일] 새로 그린 뒤 정렬·fit 을 같은 작업 안에서 — 첫 프레임부터 상한·높이 확정 */\", \"    try{_srailAlignTop();}catch(_e){} try{_bizFitViewport();}catch(_e){}   /* [260902 좌측 틀 통일] 새로 그린 뒤 정렬·fit 을 같은 작업 안에서 — 첫 프레임부터 상한·높이 확정 */\\n    try{ if(typeof _bizStripCountUp==='function')_bizStripCountUp(); }catch(_e5){}   /* [260903 지표 띠 카운트업] 새로 그린 면 */\"], [\"  if(_mainView==='biz'){ try{_bizInlineRender();}catch(_e){console.warn('[bizm]',_e);} try{_railYrmRender();}catch(_e){} }\", \"  if(_mainView==='biz'){ try{_bizInlineRender();}catch(_e){console.warn('[bizm]',_e);} try{_railYrmRender();}catch(_e){} try{ if(typeof _bizStripCountUp==='function')_bizStripCountUp(); }catch(_e5){} }   /* [260903 지표 띠 카운트업] 캘린더·고객 관리에서 대시보드로 */\"]]")
for P,R in [('public/standalone.html',RM+RB),('tools/memberhub_module.js',RM)]:
    s=io.open(P,encoding='utf-8').read()
    for k,(a,b) in enumerate(R):
        c=s.count(a); assert c==1,(P,'anchor',k,c)
        s=s.replace(a,b)
    io.open(P,'w',encoding='utf-8').write(s); print('OK v310',P,len(s))
