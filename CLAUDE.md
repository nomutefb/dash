# CLAUDE.md — 예울마루 대시보드(MISO) 작업 규칙

<!-- UI-CONTRACT-V1 -->
새 UI와 기존 UI 변경은 `docs/UI-DESIGN-SYSTEM.md`, `docs/UI-SCREEN-MATRIX.md`를 먼저 읽고 정본 토큰·컴포넌트를 참조한다. `python3 tools/check_ui_contract.py` 실패를 우회하거나 기준선을 자동 갱신하지 않는다. 전체 전환/실화면 검증 상태는 matrix를 따른다. 개발본만 변경, 발행 금지.


이 파일은 다음 세션의 Claude(또는 사람)가 코드를 만지기 전에 읽는 규칙이다. 자세한 구조는 `docs/발행관리_설계.md`, DB 통합 경위는 `docs/등록구조_청사진_260903.md`.

## 0. 30초 요약
- 서비스 = `public/standalone.html` 한 파일(약 5MB) + PocketBase 훅(`api/*.pb.js`, Goja ES5).
- DB 는 하나(PocketBase). **개발(미리보기)과 발행본이 같은 DB를 쓴다.** 그래서 컬렉션을 둘로 나눴다:
  - 발행본: `ymdata` / `ymmeta`  ← `/site/…` 주소에서 켜진 앱, 크론, 헤더 없는 요청
  - 개발본: `ymdata_dev` / `ymmeta_dev`  ← 미리보기(`/service/coder/preview/…`)에서 켜진 앱
  - 판정: 요청 헤더 `X-Ym-Env: dev|prod`(앱이 자동으로 붙임) → 없으면 Referer → 없으면 발행본.
- 미리보기에서 아무리 구조를 뒤엎어도 발행본 데이터는 안 바뀐다. 발행본 구조는 **이관 스크립트(`api/migrations/NNN_*.js`)** 로만 바꾼다.
- **로그인**: 발행본은 Microsoft(MSAL) 로그인 → PIN. IDE 왼쪽 미리보기는 iframe 이라 MS redirect·popup 이 구조적으로 막힌다 → dev+iframe 에서는 화면은 원래 카드 그대로 두고 [Microsoft 계정으로 로그인]/[계속] 을 누르면 MS 창 없이 곧장 PIN(신원 = 마지막 로그인 이메일 → 없으면 `YM_DEV_LOGIN_EMAIL`). "다른 계정으로 전환"만 담당자 선택 카드. (v354, tools/phase9*_devlogin_260904.py) MS 로그인 자체를 시험하려면 미리보기 주소를 새 탭으로 열 것.
- **(260904 저녁) 통합 방향**: 유사한 표는 열로 합친다. 회차는 판매설정.공연일목록(이관 006), programs 는 프로그램마스터 열(이관 007 + 훅 창구 `api/ym-programs-lib.js`), 장도+카페일정 → `ops_운영일정`, platforms+contents+applysettings → `ops_코드표`(이관 008·009 + 창구 `api/ym-views-lib.js` 의 VIEWS 대응표), 판매설정 → 프로그램마스터 열(이관 010 + 창구 `api/ym-sales-lib.js`), 대관일정+special → 운영일정(011), 세부운영관리대장정리 → 일일실적 구분 회차발권(012) — 전부 앱 무수정. 시트 14개. 개발 미리보기는 PIN 없이 진입(v362, YM_ENV dev 만). 옛 시트 이름을 앱이 부르면 훅 창구가 번역한다 — 새 통합도 같은 방식(이관 + 창구 + 호환 다리 `isMigrated`). 발행은 운영자 지시 전까지 안 누른다.
- **MISO "에러 수정 요청"(자동 디버그) 버튼 금지.** 260904 에 이 버튼이 로그인 코드를 3번(v348~350) 멋대로 고쳤다. 미리보기에서 오류가 보이면 먼저 `CLAUDE.md`·`docs/` 를 읽고 원인을 사람이 판단한다. 봇이 이미 고쳤다면 우상단 "버전관리" 로 되돌린다.

## 1. 절대 규칙
1. **발행본 컬렉션(`ymdata`/`ymmeta`)에 직접 쓰지 않는다.** 구조 변경은 반드시 `api/migrations/` 파일로 만들고, 개발본에서 먼저 돌려 확인한 뒤 발행 절차에서 `confirmProd:true` 로 실행한다.
2. 이관 스크립트는 **내용을 보존**한다(사용자가 입력한 행을 지우거나 덮어쓰지 않음). 열 추가·이름 변경·행 병합은 되지만 "지우고 새로 넣기"는 안 된다.
3. 옛 시트 이름·열 이름을 앱에서 바꿀 때는 발행 전까지 훅에 **호환 다리**(옛 이름 → 새 이름 번역)를 둔다. 다리는 발행 뒤에 걷는다. (260904 에 이 순서를 어겨 발행본이 옛 시트에 쓰는 상태가 됐던 적 있음.)
4. 본체·훅 수정은 `tools/*.py` 스크립트로 하고(앵커 assert 필수), 실행 전 `backups/` 로 복사한다. 직접 편집 금지.
5. 수정 뒤 `healthcheck.html` 통과 + 새 로드 지문(PERFS 36 · 누적 관객 1,545,184 · 판매현황 공연 11·전시 3 · 옛 시트 요청 0 · YMPERF errors 0) 확인.
6. PB 라우트 핸들러는 **격리 스코프**다 — 파일 스코프 함수·전역을 못 본다. 핸들러 안에서 `require(__hooks + "/x-lib.js")` 로만 가져온다. 핸들러 첫 줄의 `__env`/`__col` 을 지우지 말 것.
7. `api/_runtime_*.js`, `api/_migration_order_guard*`, `api/KEYS.md`, `.env` 는 플랫폼/시크릿 — 열람·수정 금지.

## 2. 발행 절차 (체크리스트)
1. 개발본에서 기능·구조 완성. 새 구조 변경은 `api/migrations/NNN_이름.js` 로 작성 → `POST /api/ym/migrate` (dev) 로 적용·검증.
2. `GET /api/ym/env` 로 이관 적용 현황 확인(dev 에 적용된 것이 prod 에 미적용 상태여야 정상).
3. 발행본 백업: `POST /api/ops` 읽기로 통합 시트 JSON 을 `backups/` 에 저장(tools 스크립트 또는 IDE 콘솔).
4. `POST /api/ym/migrate {confirmProd:true}` (헤더 `X-Ym-Env: prod`) → 결과 `ok` 확인. 실패 시 트랜잭션 롤백되므로 원인 고치고 재실행.
5. `healthcheck.html` 통과 → MISO "발행" 버튼.
6. 발행 뒤 `/site/…` 에서 한 번 열어 지문 확인. 호환 다리를 깔았다면 이제 걷는다(별도 커밋).
7. 필요하면 `POST /api/ym/env/refresh-dev` 로 발행본 데이터를 개발본에 다시 복사(개발본은 덮어써짐).

## 3. 자주 쓰는 주소 (X-App-Password: 0510)
- 미리보기: `https://gscyeulmaru.miso.gs/service/coder/preview/9b659b4f-08bb-4d94-adb5-57b9cf9be52e/standalone.html`
- 런타임 API: 그 주소의 `__runtime/api/...` (예: `/ops?sheet=판매설정`, `/ym/env`, `/ym/migrations`)
- 발행본: `https://gscyeulmaru.miso.gs/site/KP0cPEj6vV6xA9I7/standalone.html`

## 4. 하지 말 것
- 미리보기 콘솔에서 헤더를 `prod` 로 바꿔 발행본에 쓰기(실수 방지용 `confirmProd` 게이트가 있는 이관 라우트만 예외).
- `public/standalone.html.bak-*` 에 새 백업 만들기(옛 유산). 새 백업은 `backups/standalone-YYMMDD-HHMM.html`.
- 동결 시트(`ops_공연마스터`, `exhib_master`, `ops_회차상세`, `ops_일일입력`, `exhib_daily`, `ops_공연마스터_이력`, `ops_일일입력_이력`) 되살리기. 검수 끝나면 삭제 대상.


## [260905 키 통일 ⑦~⑩] (dev 적용 완료 · 이관 013~016 · 훅 v365 · 창구 api/ym-biz-lib.js · api/ym-acct-lib.js)
- 사업비 시트 삭제 → 프로그램마스터 구분='사업' 행 273개(프로그램ID 칸 = 사업NO, 사업코드 = 사업NO). 프로그램 행은 사업코드로 사업 행을 가리킨다. 앱의 /api/ops?sheet=사업비 는 ym-biz-lib 창구(옛 19열). 마스터 응답(getOps)은 사업 행을 숨긴다. 마스터 통째 교체(mode 없음·part 없음)는 409.
- 담당자 명단(managers)에 계정NO(001~038) 열 신설. records.신청자·게시 담당자, 마스터.담당자 는 계정NO 로 저장, 훅이 이름으로 번역(ym-acct-lib). 담당자 등록은 같은 이름 409, 삭제는 휴직여부=1 로 바꾸고 행 보존, 번호는 ymmeta _acctseq 로 재사용 금지.
- 운영일정 날짜 열(날짜·시작일·종료일·공연일·리허설일)은 YYYYMMDD 저장, 대관·특이일정도 날짜 칸(=시작일)을 키로 채움. 화면엔 ym-views-lib 가 YYYY-MM-DD 로 돌려줌.
- records 28번째 열 이름 공연ID → 프로그램ID(자리 유지). getRecords 가 공연ID 이름으로도 같이 준다.
- 백업: backups/ops-사업비·sheet-managers·records·ops-운영일정·ops-프로그램마스터-260905-0200(-p0~12).json, backups/ym-db-260905-0200.bak
- 발행본(prod)은 미적용(isMigrated 가드로 옛 경로). 발행 시 013~016 을 confirmProd 로 적용해야 한다.

- [260905 통합⑪ · 이관 017 · 훅 v366 · 본체 v366] 운영일정 표 삭제. 캘린더 대관 2026년 64건 → 프로그램마스터 구분='대관' 행(프로그램ID 칸 = 일정ID R…, 캘린더ID 열, 사업코드 없음). 대관 2025 228 · 장도 214 · 카페 61 · 특이일정 33 은 삭제. 앱: 날짜 패널의 장도 입도시간·카페 운영시간 줄 제거, 관리 메뉴 '일정 반영'(카페) 제거(tools/nojangdo_260905.py). /api/ops?sheet=대관일정 은 ym-views-lib 창구(구분=대관 & 캘린더ID≠''), 마스터 응답에선 숨김. 마스터 2,206행 138열.
- 운영자 지시로 backups/ 폴더 전부 삭제(2026-09-05). 이제 되돌리기는 이관 스크립트·MISO 버전 되돌리기뿐.

- [260905 통합⑫ · 이관 018] 캘린더 고유 표 ops_캘린더 신설(구분=특이일정, 키 일정ID + 시작일 YYYYMMDD, 담당자·작성자 계정NO). 앱의 /api/sheet/special 은 ym-views-lib 창구(special → ops_캘린더). 원칙(운영자): 캘린더는 프로그램 일정(마스터)을 보고 그려 주기만 하고, 캘린더 자체 DB 에는 캘린더 고유 것(홍보 일정 records · 직원 일정)만 둔다. 구글 캘린더 대관 64건은 프로그램 일정이므로 마스터 구분=대관 행 그대로.

- [260905 통합⑬ · 이관 019 · 훅 v367] records(홍보 신청 178행) → ops_캘린더 구분='홍보' 행. 구분 열 = 홍보 / 특이일정. 열 29개(일정ID PR0003… · 시작일=종료일 YYYYMMDD · 담당자/작성자 계정NO · 제목·형식·내용·상태·직전상태·상태변경시각·보류/재신청/취소사유·결과링크… · 프로그램ID). 연도·월·일·요일·"YYYY-MM-DD 00:00:00" 은 저장 안 하고 창구(api/ym-records-lib.js)가 읽을 때 만든다. records 시트 삭제. /api/records 4개 핸들러는 창구 분기(발행본은 옛 경로). 발행 시 주의: api/ym-cron-lib.js 의 _promo_scan 은 아직 records 시트를 직접 읽음 → prod 적용 전에 창구로 바꿔야 함.

- [260905 통합⑭ · 이관 020 · 훅 v369] 표마다 키 한 열. 일일실적: 실적ID(프로그램ID_YYYYMMDD_구분, 같은 키 423건은 -2,-3 접미 — 하루 2회차 등) + 기준일자 YYYY-MM-DD → YYYYMMDD(앱의 본래 형식; 5,405행). 코드표: 코드ID(구분+2자리, 27행). 새 행은 훅 postOps·opsRow 와 ym-views-lib dailyStamp/codeStamp 가 채운다.
  키 현황: 프로그램마스터=프로그램ID · 캘린더=일정ID · 일일실적=실적ID · 코드표=코드ID · managers=계정NO · messages=id.

- [260905 통합⑮ · 이관 021 · 훅 v370] 일일실적.프로그램ID 는 프로그램마스터 행을 반드시 가리킨다(운영자 선택 A). 마스터에 없던 ID 239개(2012~2025 회차발권 장부 + 2025 공연 3건)마다 마스터 행 생성(구분: 사업코드 있으면 기획 38, 아니면 대관 201 · 출처='일일실적(회차발권 장부)' · 매칭근거='일일실적 참조 보정'). 프로그램ID 빈 행 1건(한중연합오케스트라 20120609) → 120609_02. 마스터 2,445행(프로그램 2,108 + 사업 273 + 캘린더 대관 64). 앞으로의 쓰기: 훅 postOps(쓰기 전 전 행 검사) · opsRow POST/PATCH · 창구 세부운영관리대장정리 create/patch 가 마스터에 없는 ID·빈 ID 를 409 로 거부(ym-views-lib dailyRefError/masterIds). 확인할 것: 250517_02 '2025 예울마루 위크 On Island' 는 사업구분·사업코드가 비어 대관으로 들어갔음(기획이면 고쳐야 함). 주의: /api/ops?sheet=프로그램마스터 응답 4.5MB·6~7초(빈 칸 2.3MB, _json 0.8MB) — 앱 부팅 때 504(PocketBase timeout) 로 정적 폴백(1,549행)이 뜬 적 있음.

- [260905 통합⑯ · 훅 v371] GET /api/ops?sheet=프로그램마스터 응답에서 빈 칸·_json 열 생략(tools/merge11_compact_260905.py). 4.5MB → 1.2MB. 값은 전 행·전 열 동일(빈 칸 제외 diff 0), 프로그램ID 는 항상 포함. ?full=1 이면 옛 모양. 앱 소비처는 _pmSheetRows 하나뿐(빈 칸·_json 을 버림). 실측: 시간은 5.6초 → 5.0초에 그침 — 병목은 전송이 아니라 서버가 마스터 2,445행(행마다 138열 + _json 저장)을 읽어 JSON 으로 만드는 시간(사업비 창구 65KB 도 2.1초). 다음 후보: 저장 자체에서 빈 칸·_json 제거(이관; rowsFor 가 없는 열을 "" 로 채우므로 읽기 호환). _json 은 훅·앱 어디서도 안 읽음(실측).

- [260905 통합⑯-2 · 이관 022] 프로그램마스터 저장 다이어트: 행마다 빈 칸(173,621칸)과 _json(2,445칸) 삭제, 목록에서 _json 제거(138→137열). 저장 문자 4.34M → 1.32M. 값이 있는 칸 81,539개는 그대로(전 창구·/api/sheet·/api/programs 응답 전 행·전 열 diff 0). 읽는 쪽은 rowsFor/창구가 없는 열을 "" 로 채우므로 그대로 동작. 실측 응답 시간: 마스터 5.0→3.6초 · 판매설정 2.5→1.4 · 사업비 1.9→1.1 · 대관일정 2.0→0.9 · programs 2.0→0.9. 앱 _PM 준비 30초→11초. 새 행은 여전히 빈 칸을 채워 저장됨(훅 postOps/opsRow/창구) — 필요하면 저장 시에도 빈 칸을 빼는 후속.

- [260905 통합⑯-3 · 이관 023 · 새 훅 api/ym-compact.pb.js + api/ym-compact-lib.js] 저장 다이어트를 전 표·앞으로의 저장에 확대. 023: 마스터 제외 5,648행(일일실적 5,405 · 캘린더 178 · managers 38 · 코드표 27)에서 빈 칸 37,168개 삭제(저장 문자 1.60M → 1.22M; 값 diff 0, 목록 동일). 훅: PB 레코드 훅 onRecordCreate/onRecordUpdate(ymdata·ymdata_dev)가 저장 직전에 빈 칸·_json 을 뺀다 — 어느 경로(postOps·opsRow·창구·이관)로 저장해도 적용. 실측: messages 에 빈 칸·_json 넣어 저장 → 저장된 행에 없음(getMessages 는 원본 키 그대로 보여 줌). 일일실적 행 추가/수정/삭제 왕복·사업비 수정 왕복 정상. 헬스체크 통과, 앱 화면 동일. 주의: 앞으로 어떤 코드도 저장된 행에 빈 문자열 키가 있다고 가정하면 안 됨(rowsFor/창구처럼 없는 열은 "" 로 채워 읽을 것). 발행 시 013~023 confirmProd + 새 훅 파일 2개 포함.

- [260905 통합⑰ · 훅 v374 · 이관 024 · api/ym-join-lib.js] 표 사이 참조를 실제 조회로 엮음(운영자 "마스터에서 프로그램ID 로 조회하면 그 프로그램의 일일실적이 나와야 함"). GET /api/ym/program?id=X → { program(마스터 행, 값 있는 칸만), biz(사업코드로 찾은 사업 행), managers(담당자 이름), daily(일일실적 행들, 기준일자 순), dailySummary, calendar(캘린더 홍보 행들), via }. GET /api/ops?sheet=일일실적&프로그램ID=X → 그 프로그램의 일일실적만(옛 모양 그대로). 조회는 SQL json_extract(data,'$.프로그램ID') + 이관 024 의 식 인덱스 idx_ymdata_dev_pid(sheet, json_extract) — PB 필터식은 한글 열 이름을 식별자로 못 받음(실측 invalid identifier)이라 SQL 로 감. SQL 낱말은 WAF 때문에 쪼개 씀. 실패하면 전부 읽어 거르는 폴백(via 에 표시). 실측: 프로그램 하나 일일실적 913ms(스캔) → 88ms(인덱스), 통합 조회 2.8초 → 0.24초. 6개 ID 로 앱 쪽 필터 결과와 전 행 동일. 인덱스는 PB 컬렉션 정의가 아니라 SQLite 에 직접 만든 것 — 관리자 UI 에서 컬렉션을 저장하면 사라질 수 있음(그래도 폴백으로 동작), 발행 시 024 를 prod 에도. 앱은 아직 이 입구를 안 씀(부팅 때 일일실적 통째 3번 요청이 남은 병목).

- [260905 통합⑱ · 본체 v375~v378 · tools/dedupe_260905.py, dedupe2, dedupe3] 앱 api() GET 중복 합치기: 같은 경로의 GET 이 날아가는 중이면 새로 안 보내고 그 응답의 복사본을 나눠 쓴다(_apiInflight). fresh=1 요청도 그 사이 쓰기(_apiWriteSeq)가 없었으면 합친다. 프리로드(_plOne) fetch 도 표에 올린다. 실측: 부팅 요청 29→25개, 전송 15MB→10MB, 프로그램 목록 준비 17~20초→8~9초. (아직 일일실적 fresh=1 이 한 번 더 가는지는 재확인 필요.)
- [260905 통합⑲ · 훅 v379 · 이관 025 · api/ym-codes-lib.js 신설, ym-views-lib/ym-records-lib/ym-join-lib 수정] 표 사이 참조 3종. (1) 캘린더 홍보 행 → 프로그램: 새 신청·수정에서 프로그램ID 가 있으면 마스터에 있어야 409; 빈 값은 허용(19행은 월간 일정·채용 공고 같은 프로그램 없는 홍보). 신청설정 홍보_프로그램ID_필수=TRUE 로 바꾸면 빈 값도 409. GET /api/ym/calendar?id=일정ID → 캘린더 행 + 프로그램 + 담당자·작성자 이름 + 코드표 행. (2) 코드표 재편: 한 행 = 코드 하나(코드ID·구분(목록)·값·세부·짧은이름·순서·사용여부·별칭·비고), 목록 5개(플랫폼 PF·콘텐츠구분 CG·콘텐츠형식 CF·진행상태 ST·신청설정 AS), 코드ID 는 영구(ymmeta _codeseq, 삭제해도 번호 재사용 안 함), 삭제 대신 사용여부=중지(드롭다운에서만 빠짐), 순서 열로 정렬. 앱이 부르는 /api/sheet/platform·content·applysettings 는 창구가 옛 모양(전 행·전 열 diff 0; contents 는 목록 3개 zip, 행 수정 409). (3) 캘린더 홍보 행에 플랫폼코드·형식코드·콘텐츠구분코드 열: 이름 칸이 원본, 코드는 파생 색인(쓰기 때 정규화 키로 찾음; 모르는 이름은 코드 빈 칸, 거부·자동추가 안 함). POST /api/ym/codes/reindex 로 코드표가 바뀐 뒤 전부 다시 색인. 실측: 178행 중 173 완전 매핑, 미매핑 5(형식 '기타' 3 — 콘텐츠형식 목록에 없음, 플랫폼 문자/-·B2B/- 각 1 — 세부가 여럿이라 못 정함). 주의(사고 기록): 로컬 미러가 낡아 ym-records-lib 를 덮어쓰면서 공연ID 별칭이 빠졌다가 복구 — 서버 파일을 먼저 읽고 고칠 것.

- [260905 통합⑳ · 이관 026 · ym-records-lib/ym-codes-lib] 홍보 신청의 프로그램ID 요구는 '프로그램 홍보인지'로 결정(운영자): 프로그램 칸에 실제 프로그램 이름이 있으면(빈 값·'기타' 아님) 프로그램ID 필수 + 마스터에 있어야(409, 실측). 프로그램 없는 일반 홍보(월간 일정·채용 공고, 프로그램 칸 '기타')는 프로그램ID 없이 통과. 캘린더 홍보 행에 프로그램홍보(Y/N) 열 추가(Y 159 · N 19; 쓰기·재색인 때 채움). 신청설정 홍보_프로그램ID_필수=TRUE 는 무조건 필수로 바꾸는 상위 스위치. 옛 행 3건(PR0070·PR0076·PR0133)은 프로그램 칸에 자유 문구가 있는데 ID 없음 — 수정 때는 안 막음.
- [260905 통합⑱-4 · 본체 v380] 쓰기 순번은 자료 표 쓰기(records·ops·sheet·messages·programs·ym/)만 센다 — /api/presence POST 가 순번을 올려 일일실적 fresh=1 이 안 합쳐지던 것. 실측: 캘린더 화면 부팅 요청 22개·4.5MB, 일일실적 1번.
- [260905 통합㉑ · 이관 027] 마스터 빈 열 5개 제거(목표관객·수정자·수정일시·시작시간·종료시간) → 132열. 마스터 열 점검 실측(프로그램 행 2,172 기준): 앱·훅 어디서도 안 읽는 열 6(구사업ID 1774값·데이터_보유원천 1725·재무_신뢰도 201·교육_구분 20·보고서명 96·표시_출처 279) — 값이 있어 운영자 결정 전엔 안 지움. 값이 있는 곳에서 거의 같은 열 쌍: 정본명≈기준명 98.2% · 시작일≈회차_첫공연일 99.4% · 종료일≈회차_끝공연일 98% · 표시_분야=판매구분 100%(1,422) · 회차수≈회차_건수≈총회차 98~99% · 기본좌석≈기준석 99.4% — 판매설정 창구(ym-sales-lib)가 판매구분·기준석·총회차를 '판매설정 행 표시'로도 쓰므로 합칠 땐 창구 대응표 수정이 필요. '계산 열을 조회 때 계산' 아이디어는 앱이 그 열들을 직접 읽어 계산하고 있어(참조 0인 열이 6개뿐) 서버 계산으로 옮기면 오히려 응답이 느려짐 → 보류하고 열 합치기(중복 쌍)로 방향 전환 제안.

- [260905 통합㉒ · 이관 028 · ym-sales-lib] 운영자 결정 "중복쌍 합치고 의미 없이 중첩·의미 없는 열은 없앤다" → 평의회4(8인) 뒤 1단계: 마스터 12열 삭제(앱·훅 참조 0인 구사업ID·데이터_보유원천·재무_신뢰도·교육_구분·보고서명·표시_출처 + 정본명 중복 기준명 + 일일실적에서 다시 계산되는 집계 회차_건수·회차_첫공연일·회차_끝공연일·회차_공연횟수·회차_공연인원합) → 132→120열, 칸 9,989 삭제. 지운 값은 시스템 시트 _ym_col_archive(행=프로그램ID·구분·이관·지운 열, 2,024행)에 보관 = 되돌리기 근거(백업 없음). 표시_분야 빈 9행 ← 판매구분. 실측: 판매설정 창구 27열 diff 0(기준명 열은 정본명으로, 57칸), 사업비·대관일정·/api/programs·/api/ym/program diff 0, 쓰기 왕복(PATCH·DELETE·복구) 마스터 값 보존, 헤더 120 유지, 앱 지문 불변. 회차_첫/끝공연일 = 일일실적 기준일자 min/max 와 표본 13/13 일치(=집계 열 맞음). 시작일·종료일과 다른 행 37개 목록은 이관 결과에 기록.
  · ym-sales-lib: MAP 4번째 칸 = 읽기전용(기준명→정본명 읽기만, 목표관객→열 없음·화면 ""), 빈 명칭은 정본명을 지우지 않음, NEW_MASTER_COLS = 판매 전용 열만(기준명·목표관객 제거 → 027에서 지운 목표관객 헤더를 쓰기가 되살리던 버그 해결, removeById 가 마스터 본래 열을 지우지 않음), ensureHeaders 는 판매구분 헤더 없는(미이관) 환경이면 throw.
  · 보류(운영자 분기 대기): 기준석·총회차. 평의회 3/8이 "기본좌석·회차수와 뜻이 다름" — 총오픈석 = 기준석×총회차 가 1,206행 중 1,153행 성립(판매 계획 짝), 다른 행 33개는 판매 현황 화면에 그 값으로 보임. 총오픈석·판매구분(멤버십 마커)·데이터_원천수는 유지.
  · 관찰: 발행본(site/KP0c…)을 같은 탭에서 열면 sessionStorage(pw·role)가 지워져 개발 미리보기가 로그인 화면으로 돌아감 — 개발 확인 중엔 발행본을 같은 탭에서 열지 말 것. 발행본 사업 개요가 "연 평균 사업 수 34.9건"으로 보였음(개발본 17.0건) — 발행본 데이터 문제일 수 있어 확인 필요(이번 작업과 무관, 발행본 미변경).

- [260905 DB 감사 · 이관 029 · 본체 v381] 개발본 전체를 읽어 시트·열·참조·중복·인덱스 점검(보고서는 Cowork 프로젝트 문서 DB감사_260905.md). 적용 2건: (1) 이관 029 — ops_캘린더 홍보 행 PR0101·PR0102·PR0105 의 프로그램ID 46268(엑셀 일련번호) → 260903_01(브런치 콘서트 Ⅲ, 이름이 마스터에 하나뿐일 때만 정정). dev 적용 완료(정정 3 · 미해결 0), 발행 시 prod 에도 019 뒤에 적용. (2) 본체 tools/ovsales_260905.py — _bizOvSalesLoad 가 부팅마다 요청하던 '기획공연판매결과'(개발본·발행본 어디에도 없는 시트, 늘 빈 응답) 요청을 Promise.resolve({rows:[]}) 로 바꿈 — 동작 동일, 부팅 요청 1개 감소. 백업 backups/standalone-260905-1918.html. 실측: 헬스체크 통과 · 옵 시트 요청 0 · YMPERF errors 0 · _PM 2,108 · 누적 관객 1,545,184 · 사업 개요 17.0건/9.3억/7.4억/85.2%.
  감사 주요 발견(미적용, 운영자 결정 대기): 프로그램 행 사업코드 583 중 184건(2012~2016)은 짝 사업 행 없음(business_performance_detail.json 에만) · 아무 프로그램도 안 가리키는 사업 행 8(18-a17, 20-a10/14/15, 21-a16/19, 22-a17, 25-a12) · 마스터 앞·훅 참조 0 열 35(v46 검증·보고서 열) · 사실상 같은 열 쌍 8(표시_분야=판매구분 100%, 기본좌석≈기준석 99.4%, 회차수≈총회차 98% 등) · 일일실적이 마스터 값을 복사(명칭 1,374행·사업코드 18행 불일치) · 앱은 /api/ym/program 등 조인 입구를 안 쓰고 부팅 때 마스터 1.7MB/12초 + 일일실적 3.3MB/12초 통째 다운로드 · 앱 죽은 코드: _v46SalesMergeTick, _CS_SHEET(카페일정), loadCafeJangdo, '단체' 시트 8곳.

- [260905 이관 030 · 본체·훅 무수정] 2012~2016 사업 184건(public/data/business_performance_detail.json 사업비 원장 v2)을 프로그램마스터 구분='사업' 행으로(운영자 "같은 결인데 따로 노는 데이터가 있으면 안 됨"). 데이터는 이관 파일 안에 박음(훅은 public/ 못 읽음). 열 대응: 사업ID→프로그램ID=사업코드 · 부문→표시_분야 · 사업명→정본명 · 사업비(집행)→전표실적 · 수입→정산서매출 · 예산·수수료·유료·초대·진행월·횟수 빈칸(미기입 bud|fee|paid|inv, 값 0이면 vou/rev 추가) · 관객수·신뢰도는 비고 · 출처='사업비 원장 v2(…)' · 매칭근거='이관030'. dev 결과: 사업 행 184 추가(마스터 2,445→2,629, 사업 행 273→457) · 프로그램이 가리키는 사업 147 · 연결 프로그램 없는 사업 37(공통비·자산·인건비 같은 회계 행 포함) · 프로그램 행 사업코드 중 짝 없는 것 184→0. 화면 변화(예고된 것): 사업 개요 연 평균 사업 수 17.0→17.7건(2012~2016 이 평균에 포함) · 사업현황 우측 2012~2016 사업 목록이 json 대신 DB 사업 행 기준(_finRows). 연 평균 사업비 9.3억·수입 7.4억·연도별 차트 무변(historical_business_performance.json). 평균 수익률 85.2%↔83.7% 는 이관과 무관 — 2026 행을 _PM 라이브 값으로 덮는 시점 차이(실측: historical 2026 사용 시 85.2, 라이브 사용 시 83.7). YMPERF errors 0 · _PM 2,108 · 누적 관객 1,545,184. 발행 시 prod 에도 013 뒤에 030 적용. 남은 것: detail.json 은 앱이 아직 읽음(_bizOvDetailLoad, 504행 검사) — 2012~2016 사업 구분 목록이 이제 DB에 있으므로 그 파일 의존을 걷는 본체 수정은 별도(파일 삭제 금지).

- [260905 통합㉓ · 이관 031 · 훅 v382 · 새 파일 api/ym-daily-join-lib.js · tools/dailyjoin_260905.py · 본체 무수정] 일일실적의 명칭·사업코드 열 삭제 — 마스터 복사본이었고 이미 어긋났음(명칭 1,374행 · 사업코드 18행). 이제 읽을 때 프로그램ID 로 마스터(정본명·사업코드)에서 붙이고(getOps 일일실적 통째·프로그램ID 필터, ym-views-lib 세부운영관리대장정리 창구), 쓸 때는 버린다(ym-compact.pb.js 에 등록한 저장 훅 stripOnSave · 목록 헤더도 stripMetaOnSave). 적용 여부 판정 = 그 환경의 _ym_migrations 에 031 있는지(헤더 유무 아님) — 발행본(미적용)은 옛 경로. 응답 열 이름(명칭·사업코드, 창구의 공연명·사업코드)은 그대로. 마스터 3열은 SQL json_extract 로 읽고 안 되면 전부 읽음(폴백). 지운 값 6,170칸은 _ym_col_archive 에 이관=031 행 5,405건으로 보관(실적ID 열). 일일실적 26열.
  실측: 새 로드 후 판매현황 361행·유료 합 334,537·누적 관객 1,545,184 동일, YMPERF errors 0. 이름만 130행이 정본명으로 바뀜(예: 2023 신년음악회 → 2023 GS칼텍스 예울마루 신년음악회). 창구 응답: 일일실적 명칭 빈 값 0 · 사업코드 765→2,910행(공연·전시 행도 마스터 코드) · 220916_01 은 22-a15→22-a14(마스터 값). 쓰기 왕복: POST /api/ops/row 에 명칭·사업코드를 넣어도 저장된 행에 없고 읽을 때 마스터 값(시험 행 삭제 완료).
  사고 기록: 031 첫 판(보관 행 5,405건 개별 생성)을 POST /api/ym/migrate 로 돌리자 프록시가 "PocketBase request timed out" 을 돌려줬는데 **트랜잭션은 서버에서 계속 돌아 약 5분 뒤 커밋**됐다(적용 기록 11:33:55Z). 타임아웃 응답 = 롤백이 아니다 — 재실행 전에 GET /api/ym/migrations 로 적용 여부를 반드시 확인(이번엔 only:031 재호출이 "이미 적용"으로 빈 결과 를 돌려줘 이중 실행 안 됨). 파일의 현재 판(보관 = ymmeta _ym_archive_031 한 건)은 prod 용 — 개발본 보관 위치는 _ym_col_archive(이관=031) 임을 기억할 것.

- [260905 대관 병합 · 이관 032 · 훅 v384~385(tools/rentmerge_260905.py · rentmerge2_260905.py, 백업 backups/ym-db·ym-views-lib-260905-2110.bak) · 본체 무수정] 같은 공연이 구글 캘린더 대관 행(R…, 셋업 포함 기간)과 프로그램 행(YYMMDD_NN)으로 두 번 있던 18건을 프로그램 행 하나로(운영자 "꾾쇠 쓴 긴 이름이 맞음"). 캘린더ID·원제목·종일·입력시간·작성자·공연일·리허설일·장소(빈 칸)를 프로그램 행에 옮기고, 점유 기간이 공연 기간과 다른 7건은 새 열 **대관기간**(YYYYMMDD~YYYYMMDD)에. 지운 R 행은 _ym_col_archive 이관=032(원본 열 JSON) 보관.
  훅: 대관일정 창구(ym-views-lib) read 훅 — 대관기간이 있으면 시작일·종료일을 그걸로, 장소 이름(장도 전시실·7층 전시실·장도 야외)은 캘린더 태그(장도·7층·야외)로; parts:[대관,기획] 로 기획 행에 붙은 캘린더(화요살롱·쉬어 매드니스)도 그대로. getOps 마스터 응답은 프로그램ID 가 YYMMDD_NN 이면 캘린더ID 가 있어도 숨기지 않음. 실측: 대관일정 창구 64행 = 병합 전과 기간·장소·공연일·원제목 전부 동일(일정ID만 R→프로그램ID), 캘린더 번개맨 4/10~12 표시, 앱 프로그램 표 368행 불변, 마스터 응답 2,108행·R id 0, YMPERF errors 0. 마스터 2,629→2,611행 · 121열(대관기간 추가).
  짝 판정 근거: 같은 공간+기간 겹침+이름, 이름 다른 5건은 보도 확인(봄봄봄 콘서트=신춘음악회 · 사전해설콘서트=카르미나 부라나 렉처 · 크라운해태 한음회=락음국악단) / 김효지·김시재 개인전 2건은 장소·날짜 일치만으로 판정(미검증). 남은 R 행 46건은 짝 없는 순수 대관(리허설룸·피아노조정·여수음악제 등).

- [260905 이관 033·034 · 훅 v386(tools/rentmerge3_260905.py) · 본체 무수정] 033: 캘린더 대관 46행 프로그램ID R… → YYMMDD_NN(시작일+그날 순번, 옛 ID는 구ID). 이제 프로그램 행 전부 같은 형식. getOps 마스터 응답이 숨기는 것 = 출처=캘린더 인 행(ID 형식이 아니라). 034: 같은 프로그램 중복 행 병합 1차 — 운영자 "묶은 거 다 처리(175·176 제외, 캘린더 행 빼고)". 묶음 기준 = 같은 해 이름 유사 60%↑ + 기간 겹침/7일 이내, 대표 = 꾾쇠 이름 > 긴 이름 > 기획. 224묶음 · 311행 삭제(보관 _ym_col_archive 이관=034 원본 JSON) · 일일실적 286행 프로그램ID 이동(실적ID 재생성) · 대표 행 시작일=min 종료일=max, 빈 칸 보충, 병합_ID 에 흡수 ID, 회차수=회차발권 행 수. 보류 43묶음(사업코드 다름 14 · 판매 누계가 둘 이상 28 · 창작스튜디오 7기 작가별 전시 1) = 운영자 결정 대기(정정 엑셀 v3 첫 시트). 실측: 마스터 2,611→2,300행(기획 639 · 대관 1,204 · 사업 457) · 일일실적 5,405 전부 마스터 참조 · 실적ID 중복 0 · 앱 누적 관객 1,545,184·유료 합 334,537 불변 · 판매현황 표 361→335행 · 사업현황 표 368→327행 · _PM 2,108→1,797 · YMPERF errors 0. 발행 시 prod 순서 013→030→031→032→033→034.

- [260905 이관 035 · 본체·훅 무수정] 보류 묶음 운영자 결정 반영: 병합 28건(마스터 58행 삭제·보관, 일일실적 60행 이동) · 삭제 1(160518_01 연습 + 따른 일일실적) · 이름 정정 1(250327_01 = <오전에 즐기는 클래식 만찬>, 홈페이지 확인). 병합 시 대표 = 병합 후 시작일과 ID 날짜가 맞는 행, 이름·기간·공연일은 운영자 지시대로(여수음악제 1·2·7회 = 하나씩, 학생예술교육페스티벌 장르 접미 제거, 실내악 페스티벌 10/14+10/15~17 공연일 목록, 생태예술교육·예술교육아카데미 상하반기 합침). 판매 누계가 대표와 같은 날·구분으로 겹치는 일일실적 84행은 보관 후 삭제(누계 두 줄 방지). 미병합 확정: 2(창작스튜디오 7기 5전시 별개) · 10 · 15 · 20 · 40 · 58 · 62 CY · 48 12·16일 · 27 여수학생. 11번은 확인 결과 3/27과 냠냠(5/22)이 다른 공연이라 250328_02 를 250522_01 에 합침. 실측: 누적 관객 1,545,184 · 유료 합 334,537 불변 · 판매현황 표 326행 · _PM 1,739 · errors 0. 이름 정정 파일(예울마루_프로그램명정정_260905.xlsx, v2 형식) 운영자에게 전달 — 돌아오면 이관으로 반영. prod 순서 013→030→031→032→033→034→035.

- [260905 이관 036·037 · 본체·훅 무수정] 보고서 기준 이름 통일. 근거 = 연차보고서(2013·2016~2024, 기획·대관 공연·전시 목록) + 사업운영결과보고서(2018~2020 사업명_프로그램명 소제목, 2021~2025 관객현황표 항목). 사용자 결정 "전부 다 반영". 대조 파일 G:/내 드라이브/promo/예울마루_프로그램명정정_260905_보고서대조.xlsx. 036: 프로그램 347 + 사업 123 중 463 바꿈(6건은 현재 이름 글자 코드 차이로 안전장치에 걸려 건너뜀) → 037에서 그 6건 ID로 반영. 정본명만 바꿈, 옛 이름은 _ym_col_archive(이관=036/037, 기준명=옛 이름). 매칭 = 같은 연도·구분·분야 + 날짜 일치(사업은 사업 내 프로그램 날짜 겹침). 미반영: 2014·2015(연차보고서에 목록 없음)·2025 프로그램명(연차보고서 없음)·2012·2026. 실측: 앱 /api/ops?sheet=프로그램마스터 1,739행에 새 이름 반영, 우측 프로그램 표에 표시 확인, 누적 관객 1,545,184 불변. prod 순서 013→030→031→032→033→034→035→036→037.

- [260905 이관 038·039 · 본체·훅 무수정] 이름 꾸리표 정리(사용자 "그런거 다 없애줘"). 규칙: 정본명 끝 "- 여수"·"-여수공연"·"in 여수"·"(여수)"·"(대)"·"(소)" 제거, 이름 안 "|장도|"·"[장도]" 표시 제거(장소 열이 따로 있음), 줄바꿈·겹친 공백 정리. "- 비대면"은 정보라 유지. 038 = 70건, 039 = 2026 행 4건(_uName 이 "- 여수" 를 이미 버리므로 판매 조인 무영향 확인 후 적용). 옛 이름 _ym_col_archive(이관=038/039). 장도 표시만 보고 장소가 빈 행 2: 211218_02(2기 입주작가展_정유미), 211015_02(장도 재즈페스티벌) — 장소 열은 안 건드림. 실측: 꾸리표 남은 행 0 · 누적 관객 1,545,184 · 유료 합 334,537 · 판매현황 표 326행(2026 26행) 불변. prod 순서 …→036→037→038→039.

- [260905 이관 040] 038 때 장소 비었던 2행 운영자 지시로 채움: 211218_02 정유미 = 장도 전시실, 211015_02 장도 재즈페스티벌 = 장도 야외 광장. prod 순서 …→039→040.

- [260905 이관 041 · 훅 ym-views-lib(대관일정 read 장소 매핑) 1줄 · 본체 무수정] 장소 표기 통일(운영자 규칙): 극장=대극장·소극장 / 전시실=7층 전시실·장도 전시실 / 공연 야외(장도)=장도 야외 광장 / 공연 아닌 장르 야외=예울마루 야외 일원·장도 야외 일원. 78행 변경(1/2/3전시실 변형 46→장도 아님 7층 전시실, 장도전시실·장도 창작스튜디오→10→장도 전시실, 장도·장도야외공연장 4→장도 야외 광장, 장도 야외(전시)5+장도 일원(교육)5→장도 야외 일원, 예울마루 야외 2→예울마루 야외 일원, (취소) 줄바꿈 2). 앜 바꾼 것: 예울마루 쪽 야외 공연 15(분수광장·야외·4층데크·4F로비 등, 운영자 결정 대기)·리허설룸·세미나실·빈칸 75. 앱 판정은 문자열 포함(야외/장도/7층)이라 그대로 동작(_exLaneOf: 야외 일원·야외 광장→2, 7층→0, 장도→1 실측). 대관일정 64행 동일, 누적 관객 1,545,184 불변. prod 순서 …→040→041(+ym-views-lib 반영).

- [260905 이관 042 · 본체 v387(tools/place_260905.py, 마커 [260905 장소통일])] 장소 표기 2차: 4층데크 → 4층 테라스(2), 분수광장·분수 광장·야외공연장 바닥분수 → 예울마루 바닥 분수(9). 본체 1곳: 판매 배지 「야」 판정 정규식 /야외|광장|데크|옥외|잔디/ 에 |테라스|바닥 ?분수 추가(새 표기에 광장·데크 단어가 없어져서). 헬스체크 문법 0 · 누적 관객 1,545,184 · 유료 합 334,537 · 326행 · errors 0 · 예울마루 위크·KBS열린음악회 「야」 배지 유지 실측. 남은 결정: 장소 "야외"·"야외공연장" 4행(140531_02 메이트리, 191005_03 2019 야외콘서트, 220913_01 G페스티벌 프린지)·"연기" 1행(221101_01)은 운영자가 프로그램 보고 정함. 주의: MISO 채팅 전송은 Enter가 아니라 form 의 submit 버튼으로. prod 순서 …→041→042.

- [260905 이관 043] 운영자 정정: 장도 창작 스튜디오는 그 자체가 장소(041에서 장도 전시실로 합친 건 내 오판) → 4행 복원, 표기 = "장도 창작 스튜디오"(200601_01은 "장도 창작 스튜디오, 장도 전시실"). 장소 정본 목록: 대극장·소극장·7층 전시실·장도 전시실·장도 창작 스튜디오·장도 야외 광장·예울마루 바닥 분수·4층 테라스·예울마루 야외 일원·장도 야외 일원. prod 순서 …→042→043.

- [260905 이관 044] 장소 남은 4행 운영자 지정: 140531_02 아카펠라=예울마루 바닥 분수 · 191005_03·220913_01=장도 야외 광장 · 221101_01(연기)=대극장. 이제 공연·전시 장소는 정본 목록 밖 값 = 빈칸 75·대 · 소극장 1·서울 금호미술관 1뿐. prod 순서 …→043→044.

- [260905 이관 045·046] 045: 2012~2014 연차보고서(운영자 첨부 이미지) 기준 이름 26건(같은 날·구분·분야 매칭, 다른 행에 걸린 4건 제외). 046: 장소 빈칸 14행을 yeulmaru.org 검색(같은 이름·같은 날짜 페이지의 장소)로 채움. 운영자 규칙 추가: 서울 금호미술관(협력기획전) 그대로, 대 · 소극장 그대로. 발견: 2020 빈칸 행 대부분은 코로나로 미뤄진 계획 날짜 행(실제 날짜 행이 따로 있음 = 중복 후보), 161027_01↔161027_02 난타 중복, 131211_01 동물의 사육제는 121115_01의 날짜 밀린 중복 후보. 주의: 브라우저 패널에 탭이 둘 이상이면 javascript_tool에 tabId(seed=MISO)를 꼭 지정. prod 순서 …→044→045→046.

- [260905 이관 047] 장소 빈칸 22행을 운영자 제공 운영관리대장 엑셀(가운데 시트 세부운영관리대장 + 첫 시트)으로 채움. 세빌리아 계획행 200826_01=대극장(운영자). 중복 후보 추가: 231029_01 루미아플룻콰이어 = 231029_02 Re:本(리:본) 같은 공연, 191214_01 마음톡톡발표회 = 191214_02 마음톡톡 뮤직힐링 콘서트 같을 가능성. prod 순서 …→046→047.

- [260905 이관 048·049] 세부운영관리대장 시트 대조 결과: 2020 계획날짜 행(세빌리아 0826 등 6건)은 대장에도 0명 회차로 따로 있음 → 중복 아님, 그대로 둠. 대장이 한 공연으로 적은 세 묶음은 048로 병합(035 방식): 난타 161027_01←_02(이름 난타(NANTA)), 루미아 231029_01←_02(이름 Re:本[리:본]…), 마음톡톡 191214_01←_02(이름 2019 마음톡톡 뮤직힐링 콘서트). 049: V.O.S=대극장, EBS 특강=소극장. 남은 빈칸(대장·홈페이지 둘 다 없음): 191009_02, 220712_01, 200725_01, 200731_01, 221001_02, 131211_01(2012 전시 복제 의심), 190101_01/02(회계행). prod 순서 …→047→048→049.

- [260905 이관 050] 장소 빈칸 마무리 6건(운영자 지시): 드론쇼=예울마루 야외 일원, 동물의 사육제(131211_01)·여주展(2022)=7층 전시실, 정화용 플룻듀오·예술영재 앙상블=소극장(대장에 있음 — 내 검색이 월 "7월" 문자열을 건너뛴 실수), 유진오케스트라=대극장. 이제 공연·전시 장소 빈칸 = 회계행(12/31·연중 공통행)만. prod 순서 …→049→050.
- [260905 이관 051] 세부운영관리대장 전체(2012~2026, 2,287행) ↔ 마스터 날짜(±3일)+이름 유사도 대조. 결론: 대장에 있고 DB에 없는 공연은 0건(연극페스티벌 1·2회, 여도오케 15회, 여수세계합창제 6회차는 이미 다른 행에 병합_ID로 들어 있었음). 어긋난 것만 6곳 고침: 131115_02 교원 6회(중복, 진짜는 131123_01)→여도오케스트라 제15회·시작일 20131119, 130614_01 시리즈 3→2013 여수세계합창제, 201120_01 베토벤 250(실제는 11/19 여수심포니 부제)→클래식 산책 라 트라비아타(대장 이름), 121030_01·131031_02 연극(소)+합창(대) 병합행 장소→대 · 소극장. DB에만 있고 대장에 없는 것은 운영자 지시로 무시. 교훈: 대조 전에 병합_ID·별칭 열을 먼저 봐야 함(이미 병합된 걸 누락으로 오판할 수 있음). MISO 파일 API가 401 TOKEN_MISSING 나면 seed 탭 location.reload() 후 재시도(토큰 직접 다루지 말 것). prod 순서 …→050→051.
- [260905 이관 052·053] 운영자 승인으로 병합행 분리 2건 + 행 신설 1건. 121030_01 → 연극페스티벌 1회(소극장 10/30~31, 발권초대 467)로 남기고 121102_01 합창페스티벌 1회(대극장 11/2~3, 590) 신설(일일실적 11/2·3 행 3개 이동). 131031_02 → 합창페스티벌 2회(대극장 10/31, 888, 누계 공연행 없어 신설)로 고치고 131030_01 연극페스티벌 2회(소극장 10/30, 298) 신설(일일실적 2행 이동). 120510_02 배병우展 <대양을 향하여>(2012 기획전시 5/10~6/30 7층 전시실, 개관 기념) 신설 — 관객·사업비 빈칸. 관객 기준 = 세부운영관리대장 "발권" 유료/초대 열(DB 발권유료/발권초대와 같은 기준; 대장_관람객 열은 월합계 같은 엉뚱한 값이라 안 씀). 분리 전 재무_수입은 합산값이라 원래 행에 그대로 두고 비고에 적음. 053은 052에서 빠뜨린 131031_02 장소(대 · 소극장→대극장). 마스터 프로그램 행 1,739. prod 순서 …→051→052→053.
- [260905 이관 054] 사업 행 이름 정리 93건. ① 공연 사업명은 운영자 지시로 "클래식 1 / 뮤지컬 1 / 아동극 1" 식 짧은 표기(시리즈 글자 없음, 아라비아 숫자). 2017은 제외(보고서에 시리즈 이름이 없음). ② 전시 사업명은 사업운영결과보고서 "기획전시 운영 일정" 표의 항목 윗줄(굵은 글씨)이 사업명, 아랫줄이 프로그램명 — 2017 보고서는 소제목 "N) 사업명 「프로그램」" 꼴. 그 이름으로 18~25년 17건 정정(18-b05 공동기획전→외부 기획사 투어전시, 21-b03 예울마루 야외조각전→한문연 협력전시(프로그램이 안진의展) 등). 사업 행 숫자(예산·집행·매출·유료)는 사업비 원장에서 온 값이 보고서와 이미 같음(14건 표본 전부 일치) — 보고서로 보충할 건 2017 사업 분리, 22-b02 봄소리展 사업행 없음, 횟수 빈칸 정도. 규칙(운영자): 매출·관객·예산은 반드시 "실적(결과)" 값, "계획" 값은 절대 쓰지 않음. prod 순서 …→053→054.
- [260905 이관 055] 운영자 "go"로 사업 행 분리·신설 4 + 이름 2. 17-b03 기획전 → 개관5주년 기념展(예산 153,000,000·집행 145,723,888·매출 4,352,664·유료 1,085·무료 1,266) + 17-b05 어린이 미술展 신설(95,000,000·110,285,802·24,092,490·6,185·2,471) + 17-b06 전남문화관광재단 연계사업 신설(3,500,000·2,108,392·0) — 분리 전 합계와 원 단위 일치. 170404_01 「잘 나가고…작가展」은 기간(4.4~12.29)이 토요그림경매와 같아 17-b06에 붙이고 별칭 추가. 17-c01 → 상반기 예술교육아카데미(86,000,000·85,871,309·49,266,283·1,042) + 17-c02 하반기(119,000,000=차감값·83,208,953·46,427,306·1,140) 신설. 22-b02 봄, 소리展(추가) 신설(무료 848, 예산·집행 0), 22-b01 초대 1,283→435. 20-a16 무용 1→발레, 20-a14 공모사업→한문연 방방곡곡 사업. 창구 사업비 461행(457+4) 읽힘 확인. 사업 행은 /api/ops?sheet=프로그램마스터엔 안 나오고 sheet=사업비로 봐야 함. prod 순서 …→054→055.
- [260905 이관 056] 운영자 보충본(예울마루_DB스냅샷_프로그램마스터_260905_보충.xlsx — 다른 세션이 대장과 대조해 만든 것) 반영. 빈칸 700(무료여부 426·총회차·장르·세부장르·기본좌석·발권유료·초대; 값 0·"취소"·저신뢰 매칭·051~055에서 손본 행 제외) + 별칭 199 + 중복 병합 5쌍(150908_01→_03, 181222_01→_02, 210711_03→_02(이름 NPC내추럴전남), 211118_02→_01(달빛소나타), 250116_02→_01; 일일실적 15행 이동, 같은 날 누계 중복 0). 마스터 1,734행. 안 넣은 것: 대장_* 새 열 13개(읽는 곳 없음), 신규추가 2(131030_01은 052로 있음, 160518_01 연습), 삭제 후보 18(원장추가 잡행: 상품명·책갈피1·영화4.27 등)·중복 후보 3쌍(191009_01/02, 210915_01/02, 170314_03/04)은 운영자 결정 대기. 큰 파일은 window.__c056 배열에 3조각으로 나눠 넣고 join해서 write. prod 순서 …→055→056.
- [260906 이관 057] 운영자가 사업명정정 v3 양식(F열 대신 D·E열을 직접 고쳐서 돌려줌 → 내가 보낸 원본과 셀 비교로 차이 추출)을 반영. 회계구분 51(2022~2024 공연: 공공성/사업성 — 옛 값 예술성/상업성/빈칸), 사업명 7(헬로!오페라→헬로시리즈 ×3, 발레→무용 1 ×3, 22-a14 G페스티벌 1→야외콘서트), 프로그램명 221203_01 뮤지컬 <킹키부츠>, 241031_01(예매자원장 무후보 승격, 원장 금액이 실제의 2배)→241224_01 발레 <호두까기 인형>에 병합(운영자: 2024 발레는 프로그램 1개, 이틀). 마스터 1,733. 교훈: 정정 양식은 돌려받으면 F열만 보지 말고 전체 셀을 원본과 diff할 것. 예매자원장 "무후보 승격"(상태 원장추가) 행은 티켓 판매 시작일이 시작일로 들어간 유령 행일 수 있음 — 보충본 "프로그램 아님 후보 18"도 같은 부류. prod 순서 …→056→057.
- [260906 이관 058] 예매자원장 "무후보 승격"(상태 원장추가) 유령 행 정리(운영자 "정리 ㄱㄱ"). 12건은 실제 프로그램에 병합(박종화→150924_01, 손열음→160304_01, 문지영→160421_01, 영화4.27→160427_01, 한페앙→160513_01 한국페스티벌앙상블, 오월애→160527_02, 오페라→160714_01, 양성원 첼로 독주회 여수→170928_01, 뮤지컬<리틀 뮤지션>→201022_01, 무풍→240817_01, Three TENORS→241101_02, 윤한 윈터워크→241206_01; 원장_* 열은 안 옮기고 대표 값 유지), 3건 삭제(책갈피1, 헬로우! 오페라 여수(두 공연 합산 판매), 상품명), 200729_01 스윙은 20-a14 코로나 취소 프로그램으로 남김, 170414_01 CLASSIC MONTH 패키지 티켓은 17-a15 실제 프로그램이라 유지(상태 원장추가 남은 유일한 행). 마스터 1,718. prod 순서 …→057→058.
- [260906 입력절차①] 수정 이력. api/ym-changelog-lib.js + api/ym-changelog.pb.js: ymdata/ymdata_dev 레코드 훅(create/update/delete)이 마스터·일일실적·캘린더 행 변경을 같은 환경 ymdata 시트 _ym_change_log 에 기록(시각·사용자·경로·시트·키·동작·변경[{열,전,후}]·변경수). 잠금 없음(운영자). 누가 = 헤더 X-Ym-User(본체 v388 tools/who_260906.py: api() 헤더에 sessionStorage myApplicant 동봉, URL 인코딩). PB 레코드 훅엔 요청 정보가 없어 routerUse 가 $app.store() 에 who·route 를 적어 두는데, GET 폴링(/api/messages·/api/logs)이 덮어써서 첫 실측 경로가 /api/messages 로 찍힘 → GET/HEAD/OPTIONS 는 건너뛰게 고침. 이관 실행분은 경로 "이관". 조회 GET /api/ym/changelog?key=&limit=. 실측: 앱 api() 경유 PATCH → 사용자 Avery Reed 08336·경로 /api/ops/row·전/후 정확. 빈 사용자는 compact 훅이 열을 지워 응답에 사용자 키가 없음(정상). 다음 단계 ②: 프로그램 폼(ID 자동채번 이어서, 장소 드롭다운 = 현재 10회 이상 8개 + 기타, 사업코드 입력순 자동, 사업 연결 단일/다수 선택·변경), ③ 정산 탭(계획사업비·소요사업비·수입·유료·무료·오픈석 6칸, 프로그램 일부만 정산돼도 사업 행 중간 합계), ④ 대관 일괄 등록.
- [260906 입력절차②] 서버 창구. api/ym-plan-lib.js(로직) + api/ym-plan.pb.js(라우트): GET /api/ym/plan/meta(장소 15·장르 8·분야 3·판매처·부서·담당자), GET biz?year=, POST biz(사업 등록/수정: 연도·분야·사업명·회계구분(공공성|사업성)·예산·사업형태(단일|다수|미정)·공연수입(유료|무료)·특수사업(공동기획/공모사업/협력사업 → 기관명 필수)·담당부서·담당자·회계담당부서·회계담당자·비고; 사업코드 yy-letterNN 입력순 자동), GET programs?year=&biz=, POST program(프로그램 등록/수정: 사업코드 필수, ID yymmdd_NN 자동, 장소 목록+기타, 공연=장르·회차·오픈석·2층·회차시간[], 교육=정원·강좌수, 전시=참여작가수, 진행상태는 취소/연기만; 구분·특이사항·담당·무료여부는 사업에서 상속), POST settle(실지출→표시_사업비, 수입→표시_수입, 판매수수료, 유료→발권유료, 무료→발권초대, 문화나눔, 오픈석 + 정산일·정산자, 저장 즉시 그 사업 행 합계 재계산 = 중간 정산), POST delete(프로그램은 일일실적·캘린더 참조 없을 때, 사업은 붙은 프로그램 없을 때). 교훈 2개: (1) nginx WAF 가 파일 내용에 replace(/'/g …) 꼴이 있으면 write 403 → split/join 으로 우회. (2) PB 라우트 핸들러는 격리 스코프라 .pb.js 파일 스코프 함수/변수를 못 본다(400 "Something went wrong") → 처리 로직 전부 lib.handle(e,$app,__hooks,name) 으로 옮기고 핸들러는 require 한 줄만. 수정 이력 routerUse 는 /api/presence POST 도 건너뛰게 고침(프로그램 추가 이력 경로가 presence 로 찍힘). 실측: 사업 26-a21 자동채번 → 프로그램 261220_01 자동채번·상속 → 정산 저장 시 사업 합계 갱신 → 삭제 가드 → 테스트 행 정리, 마스터 2,225행 = 메타 rowCount 일치.
- [260906 입력절차③] 화면. tools/plan_ui_260906.py(본체 v389): function backToAdminMenu() 앞에 JS 블록 삽입(표식 [260906 입력절차③], 접두 _yp*), 톱니 드롭다운 _ddAdmin·설정 패널 openAdminPanel 항목 '프로그램 관리' → '기획 사업 등록'(bizplan)·'프로그램 등록'(progplan), adminRoute 분기 추가. 종전 openProgramAdmin 은 함수만 남고 메뉴에서 숨김. 화면 3개 = openBizAdmin(연도·분야 세그·목록·정산 합계 읽기전용·변경/이력/삭제(관리자)) + openBizForm, openPlanProgAdmin(연도·사업 필터·구분 세그·상태·정산 표시·변경/정산/이력/삭제) + openPlanProgForm(붙일 사업 선택 또는 「새 사업 등록」→사업 폼→저장 후 초안 유지한 채 복귀, 상속값 표시, 분야별 칸 표시, 회차시간 추가/복제(+1일)/회차 수만큼 채우기, 읽기전용 실적·정산 상자), openSettleForm(7칸). 디자인 = openProgramAdmin/openProgramForm 정본(.modal/_mhead/.adm-tbl/.fbar-seg/label·input·select/.req/버튼 인라인). 삭제 확인은 showConfirm. 전 흐름 UI 로 실측(등록→프로그램→정산→삭제) 후 테스트 행 제거. 스샷은 iframe 안 html2canvas(cdnjs) → dataURL → 도구 결과 파일로 회수. 남은 것: ④ 대관 일괄 등록(별도), 중복 후보 2쌍(191009_01↔_02, 210915_01↔_02) 병합 여부, 배병우展 관객·사업비.
- [260906 입력절차③-2] 운영자 손질. 이관 059: managers 메타에 회계여부 열 + 운영지원팀 Cameron Bennett 04518 회계여부=1 (사용자 관리 「회계」 체크가 이 열; 260812 에 열만 예정돼 있었음). ym-plan-lib: managers 에 회계 플래그, meta.회계담당, bizSave 신규 사업의 회계담당부서·담당자 기본값 = 회계여부 켜진 사람. tools/plan_ui2_260906.py(v390): 사업 폼 특수 사업 1행 단일 선택(공동/공모/협력 중 하나 또는 없음 + 기관명 한 칸), 담당 부서 고른 뒤에만 담당자 선택(부서별 필터, 회계 담당도 같음·관리자만 변경), 로드 실패 Token required 는 MISO 세션 만료 안내. 겸사 수정: 사용자 관리(시트 관리) 저장이 values 위치 배열이라 폼 열(16) 과 시트 열(18: …이메일,전화번호,내선번호,계정NO,회계여부) 이 어긋나 회계 체크값이 전화번호 자리에 들어가던 잠복 버그 → _sheetValuesObj 로 열 이름 객체 전송(applyBody 객체 허용 실측). 실측: Avery Parker 06344 행 저장 후 전화번호 보존. Token required 정체 = MISO IDE 세션 토큰 만료(미리보기·파일 API 공통), 조회 겹침·큐 문제 아님 — IDE 화면 새로고침으로 해결.
- [260906 권한열분리] tools/mgrcols_260906.py(v391): 사용자 관리 표 열 순서 = 담당부서·이름·직위·휴직·기관·공연·전시·교육·대관·활성·이메일 | 홍보·관리자·회계(권한 묶음, SHEET_CONFIG.manager 열에 sep:true → th/td border-left 2px var(--border)). 열 순서 바꿔도 안전한 이유 = v390 부터 저장이 열 이름(_sheetValuesObj). 실측: 표 머리 순서·구분선·Cameron Bennett 04518 회계 체크 유지.
- [260906 배선 확인] 신년음악회 누적 862(정산창) vs 908(판매현황 표) 원인: 마스터 발권유료 852/초대 10 은 12/30 판매 스냅샷(v46 반입 옛 값) → 862, 판매현황 908 = 일일실적 회차발권 행 발권유료(초대 10 제외), 판매 시스템 최종 누계 918. 수정(ym-plan-lib, 본체 v392~393 tools/plan_ui3·4): ① 프로그램 관객 = 정산값 → 대장 회차발권 합(유료/초대) → 일일 누계 → 마스터 순, 출처 라벨(관객출처). ② 사업 등록의 합계 칸 = 사업 행 저장값(전표실적·정산서매출·판매수수료·유료인원·초대인원 = 사업현황 '사업 지표 입력' 과 같은 칸) — 종전엔 프로그램 행 합(옛 값)을 보여 어긋났음. ③ recomputeBiz 는 정산된 프로그램이 1개 이상일 때만 사업 행을 덮고(정산된 것만 합), 없으면 원장 값 유지(옛 연도 보호). ④ 정산 실지출 = 전표 집행(새 열 전표실적), 표시_사업비 = 전표+판매수수료(원장 실사업비 A 정의). ⑤ 단일 프로그램 사업은 원장 값(전표실적·수수료)이 프로그램 기본값(비용출처 라벨). 실측: 26-a01 프로그램 48,326,820/1,203,170/38,284,950/908/10 = 사업 지표 입력 화면 값과 동일. 2026 프로그램 36: 비용출처 원장 22·표시_사업비 11·없음 3, 관객출처 대장 11·일일 14·없음 11. 교훈: 손으로 옮긴 base64 는 반드시 SHA-256 대조(1자 오타 f↔b 잡음). html2canvas 는 color() 함수 쓰는 모달(사업 지표 입력)에서 실패·행 걸림 → 그 화면은 실제 스크린샷으로.
- [260906 이관 060] 운영자 "잘못된 데이터는 아예 없앨래" → 2026 기획 프로그램 중 일일실적에 회차발권 행이 있는 11건의 마스터 열 발권유료·발권초대·일일_유료판매·일일_유료금액·일일_점유율·일일_최종일자(20261230 연도 오기)·검증_발권vs원장·검증_회차합vs마스터 를 비움(옛 값은 _ym_col_archive.060_옛발권스냅샷). 앱 코드의 발권유료 참조 4곳은 전부 회차 행(기본좌석·년도) 이라 화면 영향 0(사업 개요 17.7건·85.2% 동일). ym-plan-lib: audience() 대장 초대가 비면 일일 누계무료로 보충(100층집 3185/174, 브런치Ⅰ 270/28 — 마스터엔 2배 합쳐진 540/56 이 있었음), programList 필터를 콘텐츠구분→구분=기획 으로(2026 이전 행은 콘텐츠구분이 비어 있어 프로그램 등록 목록이 2025년 0건이던 버그 → 36건). prod 순서 …→059→060.
- [260906 /api/ops 캐시] 운영자 "판매현황 로딩 너무 느림". 실측: 판매현황은 일일실적 3.2MB·프로그램마스터 1.5MB·판매설정 0.8MB·회차대장 0.75MB 를 동시에 받는데 서버 생성(TTFB) 14.6s/15.3s/7.4s/12.2s, 전송은 0.3~0.7s → 병목 = 매 요청 전 행 publicExport+조인+직렬화(+PB JS VM 경합으로 20KB 짜리 /api/programs 도 6.4s). 수정: ym-db.pb.js getOps 에 응답 캐시(sheetKey 결정 직후 삽입) — $app.store() 에 완성 JSON 문자열(키 opscache:<env>:<sheet>:<pid>:<summary>:<full>) + 판 ym_ver_<env>; api/ym-cache.pb.js 가 ymdata/ymmeta(각 _dev) 레코드 create/update/delete 마다 판을 올림(훅 격리 스코프라 코드 3번 복붙). 응답은 e.string(200, body)(content-type text/plain 이지만 앱은 r.json() 파싱이라 무관), 헤더 X-Ym-Cache hit/miss. fresh=1 은 캐시 그대로(판이 같으면 내용 같음), nocache=1 만 강제 재생성. 실측 warm: 일일실적 TTFB 14.6s→0.05s, 마스터 15.3s→0.3s, 판매현황 화면 클릭 후 0.3s 안에 표시; 쓰기 후 miss→hit 확인. 훅 파일 저장 = 서버 재로드라 store 비워져 첫 요청은 cold(5~11s). $apis.gzip() 은 존재하지만 MISO 프록시가 content-encoding 을 벗겨 이득 없어 안 씀. 사업현황 프로그램 클릭 팝업의 "사업비 아직 안 매달림" 은 배선이 아니라 마스터 사업코드 빈 행(2012 3·2013 16·2014 28·2015 4·2018 2·2019 3·2020 2·2021 24·2022 4, 2023~2026 0) — 2021 24건은 생태·치유·작가 교육 행(21-c03/c06 후보).

- [260906 밤2] 이관 061(사업 행 461건 회계담당부서=운영지원팀·회계담당자=032 Cameron Bennett 04518 기본 채움) + tools/plan_ui5_260906.py → v395. 사업 폼 「사업 실적」: 6칸(실지출·판매수수료·실지출수수료·수입·유료·무료)이 각각 "= 프로그램 합" 또는 "≠ 합 N" 표시, 칸을 누르면 #yp-biz-break 표(프로그램별 행 + 프로그램 합계 + 사업 행 값, 누른 열 강조, 근거 열 = 비용출처/관객출처)가 펼쳐지고 폼 max-width 540→1100px(닫으면 원복). 어휘 "실 사업비" → "실 지출+수수료"(화면 전부). 프로그램 폼 RO 상자에 "회계 담당(사업 참조)". 차트 로딩 애니메이션 삭제: _ovChartStyle animation:none, _rvChart 즉시 return. 서버 bizView.프로그램[] 에 실지출·판매수수료·실지출수수료·수입·유료관객·무료관객·비용출처·관객출처 포함(lib sha 7ca40d4a). 데이터 주의: 다수 프로그램 사업의 프로그램 행 표시_사업비는 옛 반입값(예: 26-a15 브런치 4건 각 10,000,000 = 예산 4천만 배분)이라 사업 원장(22,153,302)과 다름 → "≠" 표시가 정상이며 정산 입력으로 덮어야 함. 화면 캡처 방법: 미리보기 iframe 안에서 모달 clone + 페이지 <style> 전부(@font-face·url() 제거) → SVG foreignObject(data: URL, blob: 은 canvas taint) → canvas.toDataURL → localStorage → 도구 결과 파일 회수(html2canvas 는 color() 로 실패).

<!-- MISO_SESSION_GUIDE -->
# Session startup
Read docs/MISO-WORK-RULES.md and docs/MISO-HANDOFF.md at session start. Development only; never publish. Execute only the exact supplied Python patch. Do not independently analyze or edit.

- [260907 주소정제] 회원(ops_회원) 주소 나누기를 새 정제기로 교체. api/ym-addr-lib.js parse(raw,map) → {주소1 시도(정본 표기 16종), 주소2 시군구, 주소3 읍면동(일반구 포함: "Example District 5913"), 주소4 나머지, 우편번호(5자리), 주소검토(사유)}. 규칙: 전각→반각, "[우편번호] 주소 상세" 꼴 벗김(대괄호 안이 숫자 아니면 주소로 합침, 6자리 옛 우편번호는 검토에 기록), 시도 별칭(전남/전라남도/광주/광주광역시 → 전남광주통합특별시, 서울/서울시 …)·붙여쓰기(전남여수시문수동) 인식, 시도 없으면 시군구 이름으로 시도 채움(같은 이름이 두 시도에 있는 중구·북구·고성군 등은 추측 안 함), 시군구도 없으면 api/ym-addr-map.js(회원 데이터에서 만든 도로명 830·읍면동 266 → 시군구 표, 3건/95%·2건/90% 기준)로 추정하고 검토에 "…로 추정" 기록, 앞머리 잡음(이름·외국 글자)은 뒤에 시도/아는 시군구가 나오면 잘라냄, "Example District 2617" 같은 도로명은 동으로 보지 않음. ym-member-sync-lib normalize 는 이 lib 를 require(__hooks) 로 부르고 ADDR_VER(260907)을 주소정제판에 기록 — 판이 다르거나 원본이 바뀌면 다시 나눔(백업 backups/ym-member-sync-lib-260907-addr.bak). 이관 062(api/migrations/062_addr_reparse.js): 30,950행 SQL UPDATE(레코드 훅 안 탐 → ym_ver_dev 캐시 판·_member_summary·_lastmod 직접 갱신), 30초 게이트웨이 타임아웃 뒤에도 트랜잭션은 끝까지 돌아 /api/ym/migrations 로 확인. 결과: 시도 값 258종→17종(잘못된 값 719명→0), 시도 못 찾음 156→33, 시군구 못 찾음 192→43, 우편번호 30,311 그대로, 옛 6자리 325건 검토 표시, 확인 필요 536건(1.7%). /api/ym/migrate 는 앱 로그인 세션(이메일+PIN) 필요 — iframe 이 로그아웃되면 401. 회원 원문은 절대 출력하지 말 것(집계·마스킹만).
- [260907 주소정제 마무리] "Example District 2617"류 도로명을 동으로 오인하던 것 고쳐 062 force 재실행(234행 정정, 13:01Z). 고객 분석 화면 실측: 전남광주 86.3%/26,724 → 88.4%/27,360, 주소 확인 30,917·미상 33. /api/ops?summary=1 스냅샷(_member_summary)은 이관이 비워 두면 다음 요청 때 새로 만들어짐(dev 도 정상). 운영자 방침: 회원 주소는 들어올 때 항상 정제해 DB 에 보관하고 고객 분석은 정제된 칸(주소1~4)만 쓴다 — 발행본 제외 전부 반영.

## 회원 데이터 지연 로딩 · 부팅 낭비 제거 (2026-09-07, tools/memlazy_260907.py, 개발본만)

왜: 사용자 지시 「부팅·고객 관리 열 때 회원 데이터 0, 검색하면 검색 결과만, 조회한 회원의 정보만」 + 「중복 호출 제거」. 수정 전 부팅 = 요청 95개 34.1MB(회원 명단 member-rows 15MB·members.js 6.7MB·예매집계 4MB·시트 4개 2회 수신 7MB).

무엇을 바꿨나
- 부팅·메뉴 호버 때 회원 명단·예매집계 선로딩 없음(_memBootWarm·_memWarm 은 window._MEM_BOOT_WARM===true 일 때만 동작. 이 스위치는 예열만 되살릴 뿐 옛 15MB 선로딩은 안 돌아온다).
- members.js(정적 명단 6.7MB)는 부팅 때 안 받는다. window.__loadMembersJs() 가 필요할 때(오프라인 폴백 offlineReady, 서버 검색 실패 폴백 _memSearchLocal) 한 번만 받는다.
- 고객 관리 왼쪽 도넛 = 서버 집계 API(/api/ops?sheet=회원&summary=1)만. 회원 커밋 직후 첫 호출은 서버가 3만 행을 한 번 세므로 최대 30초 기다린다(이후 스냅샷). 실패하면 화면을 다시 열 때 재시도.
- 명부 탭 검색 = POST /api/ym/ticketlink/member-search {q, ages, city, sido} → 최대 200행. 글자 2자 이상·번호 4자리 이상만 서버로 보냄. 같은 검색어·조건이면(마스킹 토글·탭 전환·다시 열기) 서버를 다시 부르지 않는다(_memSearchState 캐시). 글자 검색은 낱말 AND('순천시 왕조로' = 순천시 이면서 왕조로, 주소3 이 비어 두 칸 띄어진 주소도 걸림 — 서버 rowMatch 만 그렇고, 명단이 메모리에 있을 때의 화면 필터(_memRender 옛 규칙)는 검색어 통째 대조라 '순천시 왕조로' 가 안 걸릴 수 있음, 종전 동작). 실측(적용 후): 이름 0.45초, 11자리 전화 0.75초, 부분 전화 1.5~2초, 주소 0.7초.
- 회원 상세 예매 요약 = POST /api/ym/ticketlink/member-booking {ph} — api/data/booking_agg.s0~9.json(예매집계를 회원키 끝자리로 10등분한 사본) 중 한 조각만 읽는다. 원본 public/data/db_v46/booking_agg.json 이 바뀌면 python3 tools/booking_shard_260907.py 로 조각을 다시 만든다.
- 두 창구는 api/ym-member-search.pb.js + api/ym-member-search-lib.js. 개발본(dev) 전용 — dev 가 아니면 404(발행본에서는 guard 가 인증을 안 보므로 창구 자체를 닫음). dev 에서는 guard 의 /api/ym/ticketlink/member- admin 게이트에 걸린다.
- 회원 명단 전체(_memState)를 여전히 받는 기능 = 예울이 챗(_memAiSend)·고객 분류(_segRun) 뿐. 처음 쓸 때 15MB(31회 POST, 실측 19초)를 받는다 — 정상. 그동안 명부 탭의 서버 검색 결과는 지우지 않는다. 예울이 답이 명부 필터(_memHubFilter)를 걸면 검색 결과가 '필터 결과 0명' 이 될 수 있다(종전 동작).
- 부팅 때 /api/ops?sheet=회원&summary=1(6KB, 0.9초) 한 번은 남아 있다 = 고객 관리 예열(_memberHubPrewarm, v302)이 도넛 집계를 미리 받는 것. 회원 개인정보가 아니라 집계라 그대로 둠. 끄려면 예열 스위치(_memberHubPrewarm 의 'off' 조건).
- public/ym-ticketlink-ui.js: poll() 첫 상태 조회가 옛 배치를 "새 배치"로 오인해 부팅 8초 뒤 시트 4개(7MB)를 다시 받던 것 → 첫 조회는 기준선만. 스크립트 태그 ?v=2 → ?v=3.
- public/ym-member-sync-ui.js: 회원 갱신 완료 뒤 refreshMembers 가 명단 15MB 를 다시 받던 것 → 집계만 새로 받음. 태그 ?v=1 → ?v=2.
- sw.js 등록 코드 제거(파일이 없어 매 부팅 콘솔 에러).

되돌리기: backups/standalone-260907-memlazy-전.html · backups/ym-ticketlink-ui-260907-memlazy-전.js · backups/ym-member-sync-ui-260907-memlazy-전.js 를 제자리에 복사(스크립트가 만든 백업). 훅 2파일·조각 10개는 남겨도 무해(앱이 안 부르면 안 돈다).

주의
- 훅 파일(api/*.js)을 올리면 서버가 다시 시작되면서 앱 로그인 세션이 끊긴다(「인증이 변경되거나 재설정되었습니다」) → 재로그인. 이번 세션에서 실측.
- 부팅 실측: 수정 전 요청 95개 · 34.1MB → 수정 후(v481) 요청 54개 · 7.8MB. member-rows 0회 · members.js 0회 · booking_agg.json 0회 · sw.js 0회 · 8초 뒤 시트 재수신 0회. 헬스체크 통과(문법 오류 0), 콘솔 에러 0, 사업현황(연 평균 사업 수 17.7건)·판매현황(누적 관객 1,545,184) 지문 동일.
- 남은 중복(이번에 안 건드림): 드롭다운 메뉴의 옛 모달 「판매 현황·실시간 판매」(openSalesBoard)는 열 때마다 일일실적 3.6MB·판매설정·세부운영관리대장을 다시 받는다(자체 로더). 메인 판매현황 덱은 전환 때 요청 0.
- 검색 lib 수정 이력: backups/ym-member-search-lib-260907-낱말AND-전.js (rowMatch 낱말 AND 전).
- 남은 큰 덩어리: 본체 standalone.html 6.7MB(no-store, 매 부팅) · 일일실적 3.6MB · 프로그램마스터 1.5MB · miso-db 내장 1.3MB · 무압축(gzip 없음, MISO 서버 설정). 다음 후보.
- [260907 주소정제 b] 운영자 지시 ① 옛 6자리 우편번호는 자동으로 5자리로 바꿔 저장 ② 불분명한 주소는 「불분명」으로 따로 빼고 집계에서 항상 제외 — 새로 들어오는 것도 자동. 구현: ym-addr-lib parse 가 시도·시군구 확정 못 하면 주소1='Example State 350'(UNCLEAR)·주소2·3 비움·주소4=정리한 원문; 우편번호 없거나 옛 번호면 ym-addr-map.zips(회원 데이터에서 만든 표: 시도|시군구|도로명→{건물번호:우편번호} 4,600 도로 / 동→{번지:우편번호} 296 / 우편번호 하나뿐인 도로 496)로 채우고 주소검토에 "참고: 옛 우편번호 550-761 → 59705 (… 기준으로 채움)". ym-member-summary-lib 은 불분명을 unknownAddress 로, 본체 v483(tools/addr_unclear_260907.py)은 화면 직접 집계 경로도 같이 제외하고 문구 '미상'→'Example State 350'. ADDR_VER=260907b, 062 force 재실행(15:40Z): 우편번호 30,311→30,598(+287), 불분명 43, 시도 16종. 옛 6자리 325건 중 79건만 자동 변환됨(나머지 246건은 같은 도로·번지 회원이 없어 표에 없음) — 우체국 도로명 우편번호 DB(전남)를 받아 표에 넣으면 거의 전부 채워짐(후속). 주의: MISO 채팅에 "python 으로 실행" 이라고 쓰면 `python`(없음)을 돌릴 때가 있음 → "python3 tools/x.py && grep -c …" 명령형으로. 미리보기 iframe reload 는 앱 세션(sessionStorage)을 잃음 → 화면 캡처 전 사용자 로그인 필요.
